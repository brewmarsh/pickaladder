## 2024-06-26 - [Backend] Resolved N+1 Firestore query bottleneck using batch fetch
**Learning:** Found multiple instances where the application fetched Firestore documents individually in a loop using `.get()`, resulting in an N+1 query performance bottleneck. Batching queries using `db.get_all(refs)` effectively mitigated this. Additionally, when using `db.get_all()`, the returned documents are not guaranteed to maintain the order of the requested references. It is necessary to map the returned documents to a dictionary by document ID to restore the original order safely.
**Action:** When working with large lists of Firestore references, always batch document fetches using `db.get_all(refs)` instead of looping over individual `.get()` calls, and explicitly map results by ID to preserve order.
## 2026-06-30 - Firestore N+1 Batch Optimization Needs Careful Mocking
**Learning:** When optimizing an N+1 query pattern by replacing a loop of `ref.get()` with `db.get_all(refs)`, it's crucial to mock `mock_db.get_all.return_value = [...]` in the unit tests alongside adjusting the type hints. Otherwise, tests depending on individual `mock_ref.get.return_value` setups will fail since `get_all` bypasses them.
**Action:** Always check the unit tests associated with the data fetching layer when changing from single fetches to batch fetches, and ensure `get_all` is mocked out with the combined list of expected snapshots.
## 2026-07-01 - Optimizing Admin Friend Graph Query
**Learning:** In the admin view, iterating over all users and making an individual query for their accepted friends creates an N+1 query problem, slowing down page loads considerably as user base grows.
**Action:** Use a `collection_group` query for the `friends` subcollection to fetch all accepted relationships in a single operation, combined with a `status` index on the `friends` collection group, dramatically reducing latency and Firestore reads.
## 2024-07-02 - [Backend] Fix N+1 query in marketplace search\n**Learning:** The `search_marketplace` method was previously fetching groups one by one inside a nested loop when searching for divisions. Since the dataset can grow as active seasons increase, this posed an N+1 query problem that could lead to latency overhead from multiple network calls. While eliminating this N+1 by batch fetching group IDs using `db.get_all(refs)` is an excellent fix, it is critical to implement the batch fetch efficiently. If the query parameters filter out many divisions (e.g. by a `query_text`), pre-filtering those divisions and accumulating only the required `groupId`s before hitting the batch fetch minimizes memory and bandwidth usage compared to unconditionally fetching all groups for every active season upfront.\n**Action:** When converting looping queries to batch queries (such as `db.get_all()`), explicitly decouple the logic into: 1. A pre-filtering phase that gathers exactly the necessary referenced IDs based on business logic, and 2. A batched fetching step using only that filtered set of IDs.
## 2026-07-07 - [Backend] Eliminated N+1 matches stream when fetching single player record
**Learning:** Found an instance in `MatchRecordService.get_player_record` where calculating a player's record involved fetching all matches for that player via a Firestore query `.where("participants", "array_contains", uid).stream()`. This dynamically calculated wins and losses, which is highly inefficient and creates an N+1 stream bottleneck when determining stats for multiple players sequentially. The `User` documents in Firestore already maintain denormalized stats containing updated wins and losses.
**Action:** When a player's record is required, use the denormalized stats directly from the `User` document instead of streaming all matches to compute it on the fly. This avoids multiple read queries and expensive stream processing.
