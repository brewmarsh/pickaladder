name: Linting

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Install uv
      uses: astral-sh/setup-uv@v5
      with:
        enable-cache: true
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        uv pip install --upgrade pip --system
        uv pip install -r requirements.txt mockfirestore coverage refurb --system
    - name: Run ruff (Blocking Checks)
      env:
        UV_SYSTEM_PYTHON: 1
      run: uv run --no-project ruff check --select UP,E,F,I .
    - name: Run ruff (Audit Checks)
      env:
        UV_SYSTEM_PYTHON: 1
      run: uv run --no-project ruff check --select ANN,D,C90 .
      continue-on-error: true
    - name: Run refurb
      env:
        UV_SYSTEM_PYTHON: 1
      run: uv run --no-project refurb .
      continue-on-error: true
