---
phase: 17-community-messaging
plan: 04
type: execute
wave: 1
depends_on: ["17-01", "17-02", "17-03"]
files_modified: [pickaladder/messaging/repository.py, pickaladder/messaging/services.py, pickaladder/messaging/routes.py, pickaladder/templates/group/management_hub.html, pickaladder/templates/messaging/chat.html, pickaladder/static/js/chat.js, firestore.rules, tests/test_messaging.py]
autonomous: true
requirements: [MESS-02]
must_haves:
  truths:
    - "Group owners can send a message to all group members simultaneously"
    - "Announcement threads appear in members' inboxes labeled with the group name"
    - "Members cannot reply to announcement threads (read-only)"
    - "Unread counts increment for all group members when an announcement is sent"
  artifacts:
    - path: "pickaladder/messaging/repository.py"
      provides: "Support for multi-participant unread increments"
    - path: "pickaladder/messaging/services.py"
      provides: "Group announcement creation and broadcast logic"
    - path: "firestore.rules"
      provides: "Security enforcement for read-only announcement channels"
  key_links:
    - from: "Group Management Hub"
      to: "/messaging/broadcast"
      via: "Broadcast Announcement button & modal"
---

<objective>
Implement group-level announcement channels (MESS-02) that allow group owners to broadcast messages to all members.

Purpose: Provide a one-way communication channel for group organizers to share updates, news, and coordination details.
Output: Broadcast capability for owners, read-only messaging interface for members, and backend support for multi-participant conversations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@pickaladder/messaging/repository.py
@pickaladder/messaging/services.py
@pickaladder/messaging/routes.py
@pickaladder/templates/group/management_hub.html
@pickaladder/templates/messaging/chat.html
@pickaladder/static/js/chat.js
@firestore.rules
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend Infrastructure for Group Announcements</name>
  <files>pickaladder/messaging/repository.py, pickaladder/messaging/services.py, firestore.rules, tests/test_messaging.py</files>
  <behavior>
    - MessagingRepository.add_message: Should increment unreadCount for ALL participants except the sender.
    - MessagingService.get_or_create_group_announcement: Initializes conversation with type='group_announcement', ownerId, and groupId.
    - MessagingService.get_inbox: Displays Group Name for group_announcement types instead of 'Other Participant'.
    - Firestore Rules: Rejects message creation in group_announcement channels if request.auth.uid != conversation.ownerId.
  </behavior>
  <action>
    1. Update `pickaladder/messaging/repository.py`:
       - Modify `add_message` to iterate through all `participants` and increment `unreadCount` for every user who is NOT the `sender_id`.
    2. Update `pickaladder/messaging/services.py`:
       - Add `get_or_create_group_announcement(db, group_id, owner_id, member_ids)`:
         - Check for existing conversation with `groupId == group_id` and `type == 'group_announcement'`.
         - If not found, create one with `participants = member_ids`, `type = 'group_announcement'`, `ownerId = owner_id`, `groupId = group_id`, and `unreadCount` initialized for all members.
       - Update `get_inbox`:
         - If `conv['type'] == 'group_announcement'`, fetch the Group name (via `GroupRepository`) and set `display_name = f"{group_name} (Announcements)"`.
    3. Update `firestore.rules`:
       - Modify `match /messages/{messageId}` `allow create` rule to include:
         `&& (get(/databases/$(database)/documents/conversations/$(conversationId)).data.type != 'group_announcement' || get(/databases/$(database)/documents/conversations/$(conversationId)).data.ownerId == request.auth.uid)`
    4. Update `tests/test_messaging.py` to cover multi-participant unread increments and broadcast creation.
  </action>
  <verify>
    <automated>pytest tests/test_messaging.py</automated>
  </verify>
  <done>Backend logic supports multi-participant unread counts and secure group announcement channels.</done>
</task>

<task type="auto">
  <name>Task 2: Broadcaster Integration & Group Management UI</name>
  <files>pickaladder/templates/group/management_hub.html, pickaladder/messaging/routes.py</files>
  <action>
    1. Update `pickaladder/templates/group/management_hub.html`:
       - Add a "Broadcast Announcement" button next to "Create New Team".
       - Implement a Bootstrap modal `#broadcastModal` with a `<textarea name="content">` and a form posting to the new broadcast route.
    2. Update `pickaladder/messaging/routes.py`:
       - Add `@bp.route("/broadcast/<string:group_id>", methods=["POST"])` route.
       - Logic:
         - Verify `g.user.uid` is the group owner or admin (via `GroupService`).
         - Fetch all member IDs for the group.
         - Call `MessagingService.get_or_create_group_announcement`.
         - Call `MessagingService.send_message` with the content from the form.
         - `flash("Announcement broadcasted successfully!", "success")`.
         - Redirect back to `group.manage_group`.
  </action>
  <verify>
    <automated>grep "broadcast" pickaladder/messaging/routes.py</automated>
  </verify>
  <done>Group owners can trigger broadcasts directly from the Management Hub.</done>
</task>

<task type="auto">
  <name>Task 3: Receiver Experience & Read-only UI</name>
  <files>pickaladder/templates/messaging/chat.html, pickaladder/static/js/chat.js</files>
  <action>
    1. Update `pickaladder/templates/messaging/chat.html`:
       - Add a conditional check: If `conversation.type == 'group_announcement'` and `g.user.uid != conversation.ownerId`:
         - Replace the `<form>` in `card-footer` with a `div` containing a message: "This is a read-only announcement channel."
         - Add an "Announcement" badge to the header.
    2. Update `pickaladder/static/js/chat.js`:
       - Ensure `scrollToBottom` is called when new messages arrive.
       - Add a check for the existence of the form before adding the `submit` event listener to prevent JS errors on read-only pages.
  </action>
  <verify>
    <automated>grep "read-only" pickaladder/templates/messaging/chat.html</automated>
  </verify>
  <done>Members see announcements as read-only threads, and the UI handles the absence of the input form gracefully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Group Owner -> All Members | Broadcast message delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Tampering | Group Announcements | mitigate | Firestore security rules enforce that only the `ownerId` can write to `messages` sub-collection if type is `group_announcement` |
| T-17-04-02 | Denial of Service | Broadcast Spam | mitigate | Limit broadcast frequency or add server-side check to ensure only legitimate owners/admins can trigger the broadcast route |
</threat_model>

<verification>
1. Log in as a Group Owner.
2. Broadcast an announcement from the Management Hub.
3. Log in as a regular Group Member.
4. Verify the announcement appears in the Inbox with the Group Name.
5. Open the chat and verify the input field is hidden and the "read-only" notice is visible.
6. Attempt to post a message via curl to the `send` route for that conversation ID as a member; verify it fails (via Firestore rules or backend check).
</verification>

<success_criteria>
- Group owners can reach all members via a single action.
- Announcements are clearly distinguished from direct messages.
- Read-only enforcement prevents clutter in announcement channels.
</success_criteria>

<output>
After completion, create `.planning/phases/17-community-messaging/17-04-SUMMARY.md`
</output>
