---
phase: 17-community-messaging
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified: [pickaladder/messaging/services.py, pickaladder/context_processors.py, pickaladder/user/routes.py, pickaladder/templates/layout.html, pickaladder/templates/navbar.html, pickaladder/static/js/notifications.js, pickaladder/static/service-worker.js]
autonomous: false
requirements: [MESS-03]
---

<objective>
Implement real-time global notifications and unread badges across the application, integrating with the existing PWA infrastructure for push support.

Purpose: Ensure users never miss a message, regardless of which page they are currently viewing.
Output: Real-time Navbar unread badges, in-app toast notifications for new messages, and FCM push registration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@pickaladder/messaging/services.py
@pickaladder/context_processors.py
@pickaladder/templates/layout.html
@pickaladder/templates/navbar.html
@pickaladder/static/service-worker.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Support for Unread Counts & Token Management</name>
  <files>pickaladder/messaging/services.py, pickaladder/context_processors.py, pickaladder/user/routes.py</files>
  <action>
    1. Update `pickaladder/messaging/services.py`:
       - Add `get_total_unread_count(db, user_id)` which sums `unreadCount[user_id]` from all conversations the user is in.
    2. Update `pickaladder/context_processors.py`:
       - Add `inject_unread_messages_count()` function and register it in the app's context processors.
    3. Update `pickaladder/user/routes.py`:
       - Add a POST route `@bp.route("/api/save_fcm_token", methods=["POST"])` that saves an FCM token to the user's Firestore document.
  </action>
  <verify>
    <automated>grep "get_total_unread_count" pickaladder/messaging/services.py</automated>
  </verify>
  <done>Backend support for unread counts and FCM token saving is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Global Real-time Notification Listener</name>
  <files>pickaladder/templates/layout.html, pickaladder/templates/navbar.html, pickaladder/static/js/notifications.js</files>
  <action>
    1. Update `pickaladder/templates/navbar.html`:
       - Add `<span id="unread-messages-badge" class="notification-badge" style="display: none;"></span>` to the "Messages" link in both desktop and mobile menus.
    2. Create `pickaladder/static/js/notifications.js`:
       - Initialize a Firestore listener on the `conversations` collection where `participants` contains the current user's ID.
       - On update, calculate the total unread count and update the `#unread-messages-badge`.
       - Detect new messages by comparing `lastMessageTimestamp` or `unreadCount` increases.
       - If a new message is received for a conversation that is NOT the one currently being viewed (check URL or global variable), trigger `showToast(msg, 'info')`.
    3. Update `pickaladder/templates/layout.html`:
       - Include `notifications.js` for authenticated users.
       - Add `firebase-messaging.js` script tag.
  </action>
  <verify>
    <automated>grep "unread-messages-badge" pickaladder/templates/navbar.html</automated>
  </verify>
  <done>Navbar badge updates in real-time and toast notifications trigger for background messages.</done>
</task>

<task type="auto">
  <name>Task 3: Service Worker & Push Notification Integration</name>
  <files>pickaladder/static/service-worker.js, pickaladder/static/js/notifications.js</files>
  <action>
    1. Update `pickaladder/static/js/notifications.js`:
       - Implement FCM token request using `firebase.messaging().getToken()`.
       - On successful token retrieval, call `/api/save_fcm_token` to persist it.
    2. Update `pickaladder/static/service-worker.js`:
       - Import Firebase scripts using `importScripts`.
       - Initialize Firebase app in the service worker context.
       - Implement `messaging.setBackgroundMessageHandler` to show browser notifications when the app is in the background.
  </action>
  <verify>
    <automated>grep "importScripts" pickaladder/static/service-worker.js</automated>
  </verify>
  <done>FCM registration is integrated and service worker handles background messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Backend | FCM Token registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Information Disclosure | Notifications | mitigate | Ensure notification payloads do not contain sensitive PII if possible; use generic "New Message from [Name]" |
| T-17-03-02 | Denial of Service | Token Spamming | mitigate | Rate limit the `/api/save_fcm_token` endpoint and validate token format |
</threat_model>

<verification>
1. Send a message to a user who is on the dashboard; verify the Navbar badge updates and a toast appears.
2. Send a message to a user who has the app closed (if FCM test environment allows); verify a push notification is received.
</verification>

<success_criteria>
- Unread messages are reflected in the Navbar immediately.
- Toasts appear for new messages when the user is on other pages.
- FCM tokens are successfully registered for PWA users.
</success_criteria>

<output>
After completion, create `.planning/phases/17-community-messaging/17-03-SUMMARY.md`
</output>
