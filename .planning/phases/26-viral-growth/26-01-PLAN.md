---
phase: 26-viral-growth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pickaladder/auth/routes.py
  - pickaladder/user/routes/profile.py
  - pickaladder/templates/share/brag_card.html
  - pickaladder/templates/group.html
  - tests/test_referral.py
autonomous: true
requirements: [GROWTH-01, GROWTH-02]
must_haves:
  truths:
    - "New users referred by another user start with 120 Social Credits (100 base + 20 bonus)"
    - "Referrers receive 20 Social Credits immediately upon successful registration of a referred user"
    - "A public route /share/brag/<user_id>/<group_id> displays a shareable Brag Card"
    - "The sharing page contains Open Graph tags with the user's current rank and streak"
  artifacts:
    - path: "pickaladder/templates/share/brag_card.html"
      provides: "Publicly shareable stat card layout"
    - path: "tests/test_referral.py"
      provides: "Automated verification of referral payouts"
  key_links:
    - from: "pickaladder/auth/routes.py"
      to: "pickaladder.user.services.credits.SocialCreditService"
      via: "adjust_balance in transaction"
---

<objective>
Implement the core viral loops for the platform: Referral rewards and social sharing.
This plan wires Social Credit payouts for referrals and creates a dedicated public page for Brag Cards with SEO-friendly meta tags.

Purpose: Drive user acquisition via social proof and incentivized referrals.
Output: Working referral reward system and a shareable stats view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pickaladder/auth/routes.py
@pickaladder/user/services/credits.py
@pickaladder/templates/group.html
@tests/test_referral.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Referral Rewards</name>
  <files>pickaladder/auth/routes.py, tests/test_referral.py</files>
  <behavior>
    - When a user registers with a `referrer_id` in session:
      - Referrer's balance increases by 20 credits (via transaction).
      - New user's initial balance is set to 120 (100 base + 20 bonus).
    - Test: `pytest tests/test_referral.py` should verify both balances after registration.
  </behavior>
  <action>
    - Modify `pickaladder/auth/routes.py` > `_handle_referral` to accept `new_user_id`.
    - Use `SocialCreditService.adjust_balance` within a transaction to award 20 credits to the referrer.
    - Update `_process_registration` to set `social_credits: 120` in the new user's document if referred.
    - Update `tests/test_referral.py` to include assertions for Social Credit balances of both parties.
  </action>
  <verify>
    <automated>pytest tests/test_referral.py</automated>
  </verify>
  <done>Referral rewards are correctly awarded and verified by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create Public Brag Card Share Page</name>
  <files>pickaladder/user/routes/profile.py, pickaladder/templates/share/brag_card.html, pickaladder/templates/group.html</files>
  <action>
    - Create `pickaladder/templates/share/brag_card.html` based on the modal layout in `group.html`.
    - Add a new route `@bp.route("/share/brag/<user_id>/<group_id>")` in `profile.py`.
    - This route MUST NOT have `@login_required` as it needs to be accessible by social media crawlers.
    - The route should fetch user stats for that group and render the template.
    - Add Open Graph (OG) meta tags in the `<head>` of the new template:
      - `og:title`: "[User Name]'s Pickaladder Stats"
      - `og:description`: "Rank #[Rank] | [Streak] Win Streak in [Group Name]. Join the ladder!"
      - `og:url`: Full URL of the share page.
      - `og:type`: "website"
    - Update the "Share Stats" button in `pickaladder/templates/group.html` to include a "Copy Link" button that copies this new public URL to the clipboard.
  </action>
  <verify>
    <automated>curl -s http://localhost:5000/share/brag/test_user/test_group | grep "og:title"</automated>
  </verify>
  <done>Public share page is live with dynamic meta tags and accessible from the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Share Page | Accessible without authentication; must only expose non-sensitive stats. |
| Referral System | Payouts triggered on registration; must prevent self-referral or bot spamming. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | referral system | mitigate | Use server-side session for referrer_id to prevent tampering in registration form. |
| T-26-02 | Information Disclosure | share page | mitigate | Only expose username, group name, rank, and streak. No PII (email, name). |
</threat_model>

<success_criteria>
- Referrals successfully award Social Credits to both parties.
- Social Credit balances are verified via automated tests.
- Brag Cards have a dedicated public URL with valid OG tags for social previewing.
</success_criteria>

<output>
After completion, create `.planning/phases/26-viral-growth/26-01-SUMMARY.md`
</output>
