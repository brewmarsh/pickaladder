---
phase: 26-viral-growth
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - pickaladder/main/routes.py
  - pickaladder/templates/user_dashboard.html
  - pickaladder/templates/layout.html
  - pickaladder/static/robots.txt
autonomous: false
requirements: [PROD-05]
must_haves:
  truths:
    - "New users see a welcome modal on their first login explaining the Rookie Quest"
    - "robots.txt exists and allows search engines to crawl appropriate paths"
    - "sitemap.xml is accessible and contains all main public routes"
    - "No references to 'ladder' remain in user-facing marketing text (Final Audit)"
  artifacts:
    - path: "pickaladder/static/robots.txt"
      provides: "Search engine crawler instructions"
    - path: "pickaladder/templates/user_dashboard.html"
      provides: "Welcome modal implementation"
---

<objective>
Finalize the user onboarding experience and perform a technical launch audit.
This plan focuses on welcoming new users, setting up SEO basics, and a final terminology verification.

Purpose: Ensure a smooth first-time user experience and search visibility.
Output: Welcome modal, robots.txt, sitemap, and verified launch state.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pickaladder/main/routes.py
@pickaladder/templates/user_dashboard.html
@pickaladder/templates/layout.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improved Welcome Flow</name>
  <files>pickaladder/templates/user_dashboard.html, pickaladder/templates/layout.html</files>
  <action>
    - Add a "Welcome to Pickaladder" modal to `user_dashboard.html` that triggers if a session flag `first_login` is set.
    - The modal should provide a high-level overview of the app and point them to the "Rookie Quest" to get started.
    - Set the `first_login` session flag in `pickaladder/auth/routes.py` during registration (or the first time they land on the dashboard).
    - Ensure the modal is visually appealing and mobile-friendly.
  </action>
  <verify>
    - Log in as a new user and verify the modal appears.
    - Close the modal and verify it doesn't appear again on refresh.
  </verify>
  <done>New users receive a warm welcome and clear direction on their first visit.</done>
</task>

<task type="auto">
  <name>Task 2: SEO Basics (robots.txt and Sitemap)</name>
  <files>pickaladder/main/routes.py, pickaladder/static/robots.txt</files>
  <action>
    - Create `pickaladder/static/robots.txt` allowing all crawlers but disallowing `/admin`, `/settings`, and other private routes.
    - Implement a `/sitemap.xml` route in `pickaladder/main/routes.py` that dynamically (or statically for now) lists public routes: `/`, `/auth/login`, `/auth/register`, `/match/leaderboard`, and public group discovery.
    - Ensure all main templates have proper `<title>` tags and meta descriptions.
  </action>
  <verify>
    <automated>curl -s http://localhost:5000/robots.txt</automated>
    <automated>curl -s http://localhost:5000/sitemap.xml</automated>
  </verify>
  <done>Site is ready for search engine indexing.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Final Launch Audit</name>
  <what-built>The entire Phase 26 viral growth and launch features.</what-built>
  <how-to-verify>
    1. Register a new user with a referral link: Verify credits for both.
    2. Share a Brag Card: Paste the link into a tool like OpenGraph.xyz or a social media draft to verify the preview.
    3. Check Dashboard: Verify the welcome modal appears for new users.
    4. Terminology Check: Search the entire codebase (templates especially) for "ladder" and ensure they are contextual (e.g., "Pickleball Ladder" is okay, but internal UI should say "Group" or "Tournament" where appropriate).
    5. SSL/Security: Verify that `SESSION_COOKIE_SECURE` and `REMEMBER_COOKIE_SECURE` are set to True in production config.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SEO Crawlers | bots hitting sitemap.xml and robots.txt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Information Disclosure | sitemap.xml | mitigate | Ensure sitemap.xml only includes public routes. |
</threat_model>

<success_criteria>
- Welcome modal successfully guides new users.
- robots.txt and sitemap.xml are correctly served.
- Final audit confirms terminology and security settings are launch-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/26-viral-growth/26-02-SUMMARY.md`
</output>
