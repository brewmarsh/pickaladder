---
phase: 05-vocabulary-transition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pickaladder/templates/index.html
  - pickaladder/templates/welcome.html
  - pickaladder/templates/login.html
  - pickaladder/templates/community.html
  - pickaladder/templates/friends/index.html
  - pyproject.toml
  - README.md
  - docs/REQUIREMENTS.md
  - docs/DESIGN.md
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-03, VOCAB-04]

must_haves:
  truths:
    - "Landing page describes 'groups and tournaments' instead of 'ladders'"
    - "Welcome template uses 'groups and tournaments' terminology"
    - "Project metadata (pyproject.toml) reflects new vocabulary"
    - "Documentation (README, REQUIREMENTS, DESIGN) uses new terminology"
    - "The 'pickaladder' branding and infrastructure strings are preserved"
  artifacts:
    - path: "pickaladder/templates/index.html"
      provides: "Updated landing page text"
    - path: "pyproject.toml"
      provides: "Updated project description"
    - path: "docs/REQUIREMENTS.md"
      provides: "Updated requirement terminology"
  key_links:
    - from: "pickaladder/templates/index.html"
      to: "pickaladder/templates/welcome.html"
      via: "Consistent messaging"
      pattern: "groups and tournaments"
---

<objective>
Implement the terminology shift from "Ladders" to "Groups/Tournaments" across the application UI, metadata, and documentation.

Purpose: Align user-facing language with the actual product use cases (Groups/Tournaments) while preserving the 'pickaladder' brand.
Output: Updated templates, metadata, and documentation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vocabulary-transition/RESEARCH.md

<interfaces>
<!-- No code interfaces affected. Pure text replacement in templates and docs. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update UI Templates (Wave 1)</name>
  <files>
    pickaladder/templates/index.html,
    pickaladder/templates/welcome.html,
    pickaladder/templates/login.html,
    pickaladder/templates/community.html,
    pickaladder/templates/friends/index.html
  </files>
  <action>
    Per VOCAB-01 and VOCAB-02:
    - In `index.html`: Update "manage your pickleball ladders" to "manage your pickleball groups and tournaments".
    - In `welcome.html`: Update "platform for pickleball ladders and tournaments" to "platform for pickleball groups and tournaments".
    - In `login.html`: Update "Run Ladders" to "Manage Groups".
    - In `community.html`: Update "It's like a ladder for pickleball" to "It's like a tournament for pickleball".
    - In `friends/index.html`: Update "It's like a ladder for pickleball" to "It's like a tournament for pickleball".
    
    STRICTLY PRESERVE: "pickaladder" branding in text and URLs. Do not change the word "pickaladder".
  </action>
  <verify>
    <automated>grep -ri "ladder" pickaladder/templates/ | grep -v "pickaladder" || true</automated>
  </verify>
  <done>All specified templates use "groups" or "tournaments" instead of "ladders".</done>
</task>

<task type="auto">
  <name>Task 2: Update Metadata & Descriptor (Wave 1)</name>
  <files>pyproject.toml</files>
  <action>
    Per VOCAB-03:
    - In `pyproject.toml`: Update `description = "A pickleball ladder application"` to `description = "A pickleball group and tournament management application"`.
  </action>
  <verify>
    <automated>grep "description =" pyproject.toml</automated>
  </verify>
  <done>pyproject.toml description updated.</done>
</task>

<task type="auto">
  <name>Task 3: Update Documentation (Wave 2)</name>
  <files>README.md, docs/REQUIREMENTS.md, docs/DESIGN.md</files>
  <action>
    Per VOCAB-04:
    - In `README.md`: Perform a context-aware replacement of "ladder" with "group" or "tournament".
    - In `docs/REQUIREMENTS.md`: Update section headers and requirement text (e.g., "Multiple Ladder Rankings" -> "Multiple Group/Tournament Rankings").
    - In `docs/DESIGN.md`: Update conceptual descriptions to reflect the transition.
    
    STRICTLY PRESERVE: Infrastructure strings and "pickaladder" branding.
  </action>
  <verify>
    <automated>grep -ri "ladder" README.md docs/ | grep -v "pickaladder" || true</automated>
  </verify>
  <done>Project documentation updated to reflect the new vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Browser | UI text is rendered here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | Configuration/URLs | mitigate | Strictly preserve "pickaladder" package name and infrastructure strings to prevent breaking Auth/Database connections. |
| T-05-02 | I | UI Text | mitigate | Review transitions to ensure "pickaladder" branding is not accidentally changed to "pickagroup" or similar. |
</threat_model>

<verification>
Ensure no non-branding instances of "ladder" remain in the specified files.
Verify that 'pickaladder' branding and infrastructure strings (Firebase bucket, etc.) are intact.
</verification>

<success_criteria>
- The application UI consistently uses "Groups" and "Tournaments" instead of "Ladders".
- Documentation matches the UI terminology.
- No functional regressions (Auth/DB) due to terminology changes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-vocabulary-transition/05-01-SUMMARY.md`
</output>
