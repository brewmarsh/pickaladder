---
phase: 28-tournament-formats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pickaladder/tournament/models.py, pickaladder/tournament/forms.py, pickaladder/tournament/routes.py, pickaladder/tournament/utils.py, pickaladder/tournament/services/tournament_service.py]
autonomous: true
requirements: [TOUR-03]

must_haves:
  truths:
    - "Tournament format (ROUND_ROBIN, SINGLE_ELIMINATION, POOL_PLAY) is persisted correctly upon creation."
    - "Standings correctly sort by wins, then head-to-head record, then point differential."
    - "Tournament mode (SINGLES/DOUBLES) is independent of format."
  artifacts:
    - path: "pickaladder/tournament/utils.py"
      provides: "Head-to-head tie-breaking logic"
    - path: "pickaladder/tournament/forms.py"
      provides: "Updated format selection with POOL_PLAY"
  key_links:
    - from: "pickaladder/tournament/routes.py"
      to: "pickaladder/tournament/services/tournament_service.py"
      via: "Service calls for creation and update"
---

<objective>
Refactor the tournament creation and standings foundation to support advanced formats.

Purpose: Distinguish between mode and format, and provide a robust standings engine for Round Robin and Pool Play.
Output: Updated models, forms, and standings logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pickaladder/tournament/models.py
@pickaladder/tournament/forms.py
@pickaladder/tournament/routes.py
@pickaladder/tournament/utils.py

<interfaces>
From pickaladder/tournament/models.py:
```python
class TournamentDict(FirestoreDocument, total=False):
    format: str  # SINGLE_ELIMINATION, ROUND_ROBIN, DOUBLE_ELIMINATION
    matchType: str
    mode: str
```

From pickaladder/tournament/utils.py:
```python
def get_tournament_standings(db, tournament_id, match_type): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tournament Mode vs Format Refactor</name>
  <files>pickaladder/tournament/forms.py, pickaladder/tournament/routes.py, pickaladder/tournament/models.py, pickaladder/tournament/services/tournament_service.py</files>
  <action>
    - Update `TournamentForm` in `forms.py` to include `POOL_PLAY` in `format` choices.
    - Modify `_handle_creation_payload` in `routes.py` to include `format` in the data sent to `TournamentService.create_tournament`.
    - Update `TournamentService._build_create_payload` in `services/tournament_service.py` to accept and store the `format` field.
    - Ensure `matchType` and `mode` are handled consistently as 'SINGLES'/'DOUBLES' (lowercase/uppercase respectively).
    - Update `TournamentDict` in `models.py` to include `POOL_PLAY` in the format type hints and add optional fields: `pool_count` (int), `promoted_per_pool` (int).
    - Refactor `Tournament.is_doubles` property to check both `matchType` and `mode` strictly.
  </action>
  <verify>
    <automated>pytest tests/test_tournament_refactor.py</automated>
  </verify>
  <done>Tournament creation persists 'format' correctly, and 'POOL_PLAY' is a valid option.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Head-to-Head Tie-breaking</name>
  <files>pickaladder/tournament/utils.py, tests/test_tournament_standings.py</files>
  <behavior>
    - Test 1: Two players with same wins/losses, one beat the other. The winner should rank higher.
    - Test 2: Three players tied. Ranking should be determined by point differential if head-to-head among the three is also tied (circular).
    - Test 3: Standings still work for elimination formats (though less critical).
  </behavior>
  <action>
    - Modify `sort_and_format_standings` in `utils.py`.
    - Before sorting, calculate a head-to-head matrix or sub-map for tied participants.
    - Update the sort key to prioritize head-to-head wins among tied groups. 
    - Note: For simplicity, if multiple players are tied, compare their direct match results if they played each other exactly once (standard for Round Robin).
  </action>
  <verify>
    <automated>pytest tests/test_tournament_standings.py</automated>
  </verify>
  <done>Standings correctly apply head-to-head tie-breaking before falling back to point differential.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → Tournament Form | Admin input for tournament configuration |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | Tournament Format | mitigate | Validate 'format' against allowed choices in forms and service layer. |
| T-28-02 | Information Disclosure | Standings | accept | Standings are public by design for tournament participants. |
</threat_model>

<verification>
Ensure tournament creation allows selecting all 4 formats and that they are saved in Firestore. Verify standings sort order manually or via test script.
</verification>

<success_criteria>
1. Tournament documents in Firestore contain `format: POOL_PLAY` when selected.
2. `get_tournament_standings` returns a list where 2-way ties are resolved by the winner of their match.
</success_criteria>

<output>
After completion, create `.planning/phases/28-tournament-formats/28-01-SUMMARY.md`
</output>
