---
phase: 28-tournament-formats
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified: [pickaladder/tournament/templates/tournaments/create_edit.html, pickaladder/tournament/templates/tournament/view.html, pickaladder/tournament/routes.py]
autonomous: false
requirements: [TOUR-03]

must_haves:
  truths:
    - "Admins can choose Pool Play and specify pool count during tournament creation/edit."
    - "Tournament lobby displays pool standings separately for Pool Play tournaments."
    - "Tournament lobby shows the bracket once pools are completed and promoted."
  artifacts:
    - path: "pickaladder/tournament/templates/tournaments/create_edit.html"
      provides: "Pool Play configuration UI"
    - path: "pickaladder/tournament/templates/tournament/view.html"
      provides: "Pool standings and bracket display"
  key_links:
    - from: "pickaladder/tournament/templates/tournament/view.html"
      to: "/tournament/<id>/generate_bracket"
      via: "Generate Bracket button"
---

<objective>
Update the UI to support advanced tournament formats and pool-based workflows.

Purpose: Provide a user-friendly interface for managing complex tournament structures.
Output: Enhanced tournament creation and lobby views.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@pickaladder/tournament/templates/tournaments/create_edit.html
@pickaladder/tournament/templates/tournament/view.html
@pickaladder/tournament/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tournament Creation UI</name>
  <files>pickaladder/tournament/templates/tournaments/create_edit.html, pickaladder/tournament/routes.py</files>
  <action>
    - Add a conditional section in `create_edit.html` that appears when `format` is `POOL_PLAY`.
    - Include fields for `pool_count` and `promoted_per_pool`.
    - Update `_handle_creation_payload` in `routes.py` to handle these new fields.
    - Add client-side validation (JS) to ensure `pool_count` is reasonable (e.g., at least 2).
  </action>
  <verify>
    <automated>Check for presence of pool_count input in rendered HTML</automated>
  </verify>
  <done>Admins can configure pool settings during tournament creation.</done>
</task>

<task type="auto">
  <name>Task 2: Update Tournament Lobby for Pool Play</name>
  <files>pickaladder/tournament/templates/tournament/view.html, pickaladder/tournament/routes.py, pickaladder/tournament/services/tournament_service.py</files>
  <action>
    - Update `view_tournament` in `routes.py` to calculate and pass pool-specific standings if `format == POOL_PLAY`.
    - In `view.html`, if `format == POOL_PLAY`, render standings grouped by `pool_id`.
    - Add a "Promote Pools to Bracket" button for admins that appears when all pool matches are finished.
    - Ensure the single elimination bracket is displayed below the pool standings once generated.
  </action>
  <verify>
    <automated>Check for pool standings section in rendered HTML for a Pool Play tournament</automated>
  </verify>
  <done>Tournament lobby correctly displays pools and provides promotion controls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Advanced Tournament Formats flow (RR and Pool Play)</what-built>
  <how-to-verify>
    1. Create a Round Robin tournament and verify pairings.
    2. Create a Pool Play tournament (e.g., 6 players, 2 pools).
    3. Verify participants are split into Pool A and Pool B.
    4. Record results for all pool matches.
    5. Click "Promote Pools to Bracket" and verify a 4-player bracket is created.
    6. Check that standings correctly reflect head-to-head results in 2-way ties.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin → Browser | Configuration of pools |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Spoofing | Admin Actions | mitigate | Ensure all promotion and generation endpoints are protected by `admin_required` and CSRF tokens. |
</threat_model>

<verification>
Manual verification of the end-to-end flow from creation to bracket generation.
</verification>

<success_criteria>
1. Round Robin, Single Elim, Double Elim, and Pool Play all work as expected.
2. UI is clean and clearly distinguishes pools.
3. Promotion logic is intuitive for admins.
</success_criteria>

<output>
After completion, create `.planning/phases/28-tournament-formats/28-03-SUMMARY.md`
</output>
