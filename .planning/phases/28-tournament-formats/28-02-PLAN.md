---
phase: 28-tournament-formats
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified: [pickaladder/tournament/services/generator.py, pickaladder/tournament/services/tournament_service.py]
autonomous: true
requirements: [TOUR-03]

must_haves:
  truths:
    - "Round Robin pairing logic is mathematically correct for even and odd participant counts."
    - "Pool Play correctly divides participants into balanced pools."
    - "Top performers from pools are correctly identified for promotion."
  artifacts:
    - path: "pickaladder/tournament/services/generator.py"
      provides: "Pool Play pairing logic"
    - path: "pickaladder/tournament/services/tournament_service.py"
      provides: "Pool-to-bracket promotion orchestration"
  key_links:
    - from: "pickaladder/tournament/services/generator.py"
      to: "Mathematical Circle Method"
      via: "Pairing algorithm"
---

<objective>
Implement core logic for Round Robin verification and Pool Play generation.

Purpose: Provide the algorithmic foundation for advanced tournament formats.
Output: Validated RR generator and new Pool Play generator.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@pickaladder/tournament/services/generator.py
@pickaladder/tournament/services/tournament_service.py

<interfaces>
From pickaladder/tournament/services/generator.py:
```python
class TournamentGenerator:
    @staticmethod
    def _get_RR_pair_ids(ids): ...
    @staticmethod
    def generate_round_robin(participant_ids): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify and Test Round Robin Pairing Logic</name>
  <files>pickaladder/tournament/services/generator.py, tests/test_rr_math.py</files>
  <behavior>
    - Test 1 (Even): 4 participants -> 3 rounds, 6 unique matches, everyone plays everyone exactly once.
    - Test 2 (Odd): 5 participants -> 5 rounds, 10 unique matches, everyone plays everyone exactly once (excluding byes).
    - Test 3 (Small): 2 participants -> 1 match.
    - Mathematical Proof (Circle Method): For $N$ participants ($N$ even), every participant $p$ meets every other participant $q$ exactly once in $N-1$ rounds.
  </behavior>
  <action>
    - Review `_get_RR_pair_ids` in `generator.py`.
    - The current implementation shifts using `[temp_ids[0]] + [temp_ids[-1]] + temp_ids[1:-1]`. This is correct for the Circle Method.
    - Add comprehensive unit tests in `tests/test_rr_math.py` to ensure zero duplicates and full coverage.
  </action>
  <verify>
    <automated>pytest tests/test_rr_math.py</automated>
  </verify>
  <done>Round Robin pairing logic is verified and tested for all participant counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Pool Play Pairing Logic</name>
  <files>pickaladder/tournament/services/generator.py, tests/test_pool_play_gen.py</files>
  <behavior>
    - Test 1: 12 participants, 3 pools -> 3 pools of 4, each pool has 6 matches.
    - Test 2: Matches are tagged with `pool_id` (A, B, C...).
    - Test 3: Participants are distributed evenly (or nearly evenly) across pools.
  </behavior>
  <action>
    - Implement `generate_pool_play(participant_ids, pool_count)` in `TournamentGenerator`.
    - Randomize/Shuffle `participant_ids` before splitting.
    - For each pool, use `generate_round_robin` to create matches.
    - Add `pool_id` to each match's metadata.
  </action>
  <verify>
    <automated>pytest tests/test_pool_play_gen.py</automated>
  </verify>
  <done>Pool Play generator correctly divides participants and generates pool-specific pairings.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Pool-to-Bracket Promotion Logic</name>
  <files>pickaladder/tournament/services/tournament_service.py</files>
  <action>
    - Implement `promote_pools_to_bracket(t_id, promoted_per_pool)` in `TournamentService`.
    - This method should:
        1. Fetch all matches and calculate standings per `pool_id`.
        2. Extract top `promoted_per_pool` players from each pool.
        3. Sort these promoted players (seeding them based on their pool performance).
        4. Call `generate_single_elimination` with the seeded promoted players.
        5. Save the new bracket matches to Firestore.
    - Wire this to be callable manually by admin or automatically when all pool matches are `COMPLETED`.
  </action>
  <verify>
    <automated>pytest tests/test_pool_promotion.py</automated>
  </verify>
  <done>Top performers from pools can be promoted into a single elimination bracket.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| System → Firestore | Saving generated matches |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Denial of Service | Generator | mitigate | Limit participant count (e.g., max 128) to prevent O(N^2) complexity issues during generation. |
</threat_model>

<verification>
Run generation tests for RR and Pool Play. Verify pool-to-bracket promotion with a mock tournament state.
</verification>

<success_criteria>
1. `generate_pool_play` produces the correct number of matches per pool.
2. `promote_pools_to_bracket` creates a Single Elimination bracket with the expected participants.
</success_criteria>

<output>
After completion, create `.planning/phases/28-tournament-formats/28-02-SUMMARY.md`
</output>
