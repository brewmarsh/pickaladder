---
phase: 18-marketplace
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: [
  "pickaladder/marketplace/models.py",
  "pickaladder/marketplace/repository.py",
  "pickaladder/marketplace/routes.py",
  "pickaladder/group/routes.py",
  "pickaladder/templates/group/view.html",
  "pickaladder/templates/marketplace/manage_requests.html"
]
autonomous: true
requirements: [MARKET-02]
must_haves:
  truths:
    - "Users see a 'Request to Join' button on groups with REQUEST join policy"
    - "Group owners can see pending requests in a management dashboard"
    - "Approving a request adds the user to the group members list"
  artifacts:
    - path: "pickaladder/marketplace/models.py"
      provides: "MembershipRequest TypedDict"
    - path: "pickaladder/templates/marketplace/manage_requests.html"
      provides: "Admin UI for approving/declining requests"
  key_links:
    - from: "pickaladder/marketplace/routes.py"
      to: "pickaladder/group/repository.py"
      via: "Add member on approval"
---

<objective>
Implement the Membership Request system to handle restricted group entry via an admin approval workflow.

Purpose: Allow group owners to vet members before they join.
Output: A complete "Request -> Review -> Approve/Decline" lifecycle.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-marketplace/18-01-SUMMARY.md
@.planning/phases/18-marketplace/18-RESEARCH.md

<interfaces>
From pickaladder/marketplace/repository.py (created in 18-01):
```python
class MarketplaceRepository:
    def search_marketplace(self, query, filters): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Membership Request Backend</name>
  <files>pickaladder/marketplace/models.py, pickaladder/marketplace/repository.py</files>
  <action>
    - Create `MembershipRequest` model in `pickaladder/marketplace/models.py` with fields: `userId`, `groupId`, `divisionId` (optional), `status` (PENDING, APPROVED, DECLINED), `message`, `createdAt`.
    - Update `MarketplaceRepository` to handle membership requests:
      - `create_request(data)`: Saves a new request to the `membership_requests` collection.
      - `get_requests_for_group(group_id)`: Returns pending requests.
      - `update_request_status(request_id, status)`: Updates the status.
  </action>
  <verify>
    <automated>python -c "from pickaladder.marketplace.models import MembershipRequest; print('Model OK')"</automated>
  </verify>
  <done>Membership request data layer implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 'Request to Join' UI</name>
  <files>pickaladder/templates/group/view.html, pickaladder/group/routes.py, pickaladder/marketplace/routes.py</files>
  <action>
    - Modify `pickaladder/templates/group/view.html` to check the group's `join_policy`.
    - If `join_policy == 'REQUEST'`, show a "Request to Join" button instead of "Join Group".
    - Implement a modal for users to write a short message when requesting to join.
    - Create a POST route in `pickaladder/marketplace/routes.py` to handle the request submission.
  </action>
  <verify>
    <automated>curl -X POST -d "message=Hello" http://localhost:5000/marketplace/request/GROUP_ID</automated>
  </verify>
  <done>Users can submit join requests from the group view.</done>
</task>

<task type="auto">
  <name>Task 3: Create Admin Approval Workflow</name>
  <files>pickaladder/templates/marketplace/manage_requests.html, pickaladder/marketplace/routes.py</files>
  <action>
    - Create `pickaladder/templates/marketplace/manage_requests.html` to list pending requests for a group.
    - Implement "Approve" and "Decline" buttons for each request.
    - Update the `/marketplace/manage/<group_id>/requests` route to handle these actions.
    - On "Approve":
      - Add the user to the `Group` members array.
      - Update the `MembershipRequest` status to APPROVED.
      - (Optional) Send a notification to the user.
    - On "Decline":
      - Update the `MembershipRequest` status to DECLINED.
  </action>
  <verify>
    <automated>pytest tests/test_group.py -k "request"</automated>
  </verify>
  <done>Group owners can manage and approve membership requests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Marketplace API | Request submission crosses here |
| Admin -> Marketplace API | Approval actions cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | `update_request_status` | mitigate | Verify that the current user is the owner of the group before allowing status updates. |
| T-18-02-02 | Elevation of Privilege | Membership Approval | mitigate | Use server-side checks to ensure only group admins can access the `manage_requests` route and trigger approvals. |
| T-18-02-03 | Spoofing | `create_request` | mitigate | Hardcode `userId` in the backend using the authenticated session ID, do not trust `userId` from request body. |
</threat_model>

<verification>
Log in as a standard user and request to join a restricted group. Then log in as the group owner and verify the request appears in the management hub. Approve the request and ensure the user is now a member.
</verification>

<success_criteria>
1. "Request to Join" button appears correctly based on join policy.
2. Requests are correctly stored in Firestore with PENDING status.
3. Group owners can approve requests, resulting in membership.
</success_criteria>

<output>
After completion, create `.planning/phases/18-marketplace/18-02-SUMMARY.md`
</output>
