---
phase: 18-marketplace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "pickaladder/group/models.py",
  "pickaladder/season/models.py",
  "pickaladder/marketplace/__init__.py",
  "pickaladder/marketplace/routes.py",
  "pickaladder/marketplace/repository.py",
  "pickaladder/templates/marketplace/index.html",
  "pickaladder/__init__.py"
]
autonomous: true
requirements: [MARKET-01, MARKET-03]
must_haves:
  truths:
    - "Users can access /marketplace and see a list of groups"
    - "Groups can be filtered by visibility (Public only by default)"
    - "The UI includes a featured carousel for highlighted groups"
  artifacts:
    - path: "pickaladder/marketplace/routes.py"
      provides: "Marketplace blueprint and landing route"
    - path: "pickaladder/marketplace/repository.py"
      provides: "Discovery queries for groups and divisions"
    - path: "pickaladder/group/models.py"
      provides: "Visibility and JoinPolicy fields on Group"
  key_links:
    - from: "pickaladder/marketplace/routes.py"
      to: "pickaladder/marketplace/repository.py"
      via: "Dependency injection/Import"
---

<objective>
Establish the foundation for the Division & Group Marketplace by expanding data models and building the core discovery interface.

Purpose: Enable users to find and browse pickleball communities.
Output: A functional marketplace landing page with search and filtering capabilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-marketplace/18-RESEARCH.md

<interfaces>
From pickaladder/group/models.py:
```python
class Group(FirestoreDocument, total=False):
    name: str
    description: str
    is_public: bool # To be replaced/augmented by visibility
```

From pickaladder/season/models.py:
```python
class Division(TypedDict, total=False):
    name: str
    participant_ids: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Group and Division Data Models</name>
  <files>pickaladder/group/models.py, pickaladder/season/models.py</files>
  <action>
    - Add `Visibility` enum (PUBLIC, UNLISTED, PRIVATE) to a new `pickaladder/core/constants.py` or similar if appropriate, or define locally.
    - Add `JoinPolicy` enum (OPEN, REQUEST, INVITE) to `pickaladder/core/constants.py`.
    - Update `Group` model in `pickaladder/group/models.py` to include `visibility: str` and `join_policy: str`.
    - Update `Division` model in `pickaladder/season/models.py` to include `visibility: str` and `join_policy: str`.
    - Note: Keep `is_public` for backward compatibility but default `visibility` based on it during data migration (if any) or initial load.
  </action>
  <verify>
    <automated>pytest tests/test_group.py</automated>
  </verify>
  <done>Models updated with visibility and join policy fields.</done>
</task>

<task type="auto">
  <name>Task 2: Create Marketplace Blueprint and Repository</name>
  <files>pickaladder/marketplace/__init__.py, pickaladder/marketplace/routes.py, pickaladder/marketplace/repository.py, pickaladder/__init__.py</files>
  <action>
    - Create `pickaladder/marketplace/` directory and `__init__.py` to define the blueprint.
    - Implement `MarketplaceRepository` in `pickaladder/marketplace/repository.py` with methods to:
      - `get_featured_groups()`: Returns groups with `is_featured` or high member count.
      - `search_marketplace(query, filters)`: Performs Firestore queries for groups where `visibility == PUBLIC`.
    - Register the blueprint in `pickaladder/__init__.py`.
    - Implement a basic `/marketplace` route in `pickaladder/marketplace/routes.py`.
  </action>
  <verify>
    <automated>python -c "from pickaladder.marketplace.repository import MarketplaceRepository; print('Import OK')"</automated>
  </verify>
  <done>Marketplace blueprint registered and repository ready for queries.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Marketplace Discovery UI</name>
  <files>pickaladder/templates/marketplace/index.html, pickaladder/marketplace/routes.py</files>
  <action>
    - Create `pickaladder/templates/marketplace/index.html` using the Volt theme patterns.
    - Implement a Bootstrap 5.3 carousel for "Featured" groups at the top.
    - Implement a grid of group cards showing name, description, member count, and a "View" button.
    - Add a search input and filter buttons (e.g., "Nearby", "Popular", "New") that update the marketplace view.
    - Ensure the `/marketplace` route passes the necessary data to the template.
  </action>
  <verify>
    <automated>curl -s http://localhost:5000/marketplace | grep "Marketplace"</automated>
  </verify>
  <done>Functional marketplace UI with discovery features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Marketplace API | Discovery requests cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Information Disclosure | `MarketplaceRepository` | mitigate | Ensure queries explicitly filter for `visibility == 'PUBLIC'` and exclude `PRIVATE` or `UNLISTED` groups. |
| T-18-01-02 | Information Disclosure | `Group` API | mitigate | Sanitize group data returned to the marketplace (e.g., don't leak member emails or private session details). |
</threat_model>

<verification>
Verify that the /marketplace route loads correctly and displays only groups marked as Public. Test search functionality with both matching and non-matching queries.
</verification>

<success_criteria>
1. Marketplace page accessible at /marketplace.
2. Search and filtering work against the Firestore backend.
3. Only PUBLIC groups are visible in the discovery list.
</success_criteria>

<output>
After completion, create `.planning/phases/18-marketplace/18-01-SUMMARY.md`
</output>
