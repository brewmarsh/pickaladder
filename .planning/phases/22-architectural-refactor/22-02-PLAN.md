---
phase: 22-architectural-refactor
plan: 02
wave: 2
depends_on: ["22-01"]
files_modified:
  - pickaladder/group/__init__.py
  - pickaladder/group/routes.py
  - pickaladder/group/routes/__init__.py
  - pickaladder/group/routes/discovery.py
  - pickaladder/group/routes/membership.py
  - pickaladder/group/routes/management.py
  - pickaladder/group/routes/stats.py
  - pickaladder/group/routes/sessions.py
  - pickaladder/group/services/group_service.py
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "Group routes are accessible at existing URLs"
    - "Group blueprint is registered and functional"
    - "Direct Firestore logic in routes is minimized"
    - "No Ruff ANN violations in pickaladder/group/"
  artifacts:
    - path: "pickaladder/group/routes/management.py"
      provides: "Group admin and member management"
    - path: "pickaladder/group/routes/membership.py"
      provides: "Joining, leaving, and inviting"
    - path: "pickaladder/group/routes/sessions.py"
      provides: "Session logging and viewing"
  key_links:
    - from: "pickaladder/group/__init__.py"
      to: "pickaladder/group/routes/"
      via: "blueprint registration/imports"
---

<objective>
Modularize Group Routes and consolidate business logic into the Service layer.

Purpose: Break down a 700+ line monolithic file and improve domain isolation and type safety for Group-related features.
Output: Refactored Group blueprint structure with specialized route modules.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CODEBASE_AUDIT.md
@pickaladder/group/routes.py
@pickaladder/group/services/group_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split Group Routes into Modules</name>
  <files>
    pickaladder/group/routes/__init__.py,
    pickaladder/group/routes/discovery.py,
    pickaladder/group/routes/membership.py,
    pickaladder/group/routes/management.py,
    pickaladder/group/routes/stats.py,
    pickaladder/group/routes/sessions.py,
    pickaladder/group/__init__.py
  </files>
  <behavior>
    - Existing group URLs (/group/, /group/<id>, /group/<id>/manage, etc.) must remain functional.
  </behavior>
  <action>
    1. Create `pickaladder/group/routes/` directory.
    2. Create `pickaladder/group/routes/__init__.py`.
    3. Distribute routes from `pickaladder/group/routes.py` into:
       - `discovery.py`: view_groups, _handle_referrer.
       - `membership.py`: view_group, join_group, leave_group, invite_friend, invite_by_email, handle_invite, resend_invite, delete_invite, request_membership.
       - `management.py`: manage_group, create_group, edit_group, delete_group, handle_membership_request, promote_member, demote_member, remove_member, _get_group_for_edit, _handle_edit_group_form.
       - `stats.py`: view_leaderboard_trend, get_rivalry_stats, get_user_group_trend.
       - `sessions.py`: quick_log, view_session, verify_session.
    4. Move private helpers like `_handle_invite_friend_form` to `management.py` or a common place if shared.
    5. Update `pickaladder/group/__init__.py` to register/import the new modules.
    6. DELETE `pickaladder/group/routes.py`.
  </action>
  <verify>
    <automated>pytest tests/test_group.py tests/test_group_leaderboard.py tests/test_session_service.py</automated>
  </verify>
  <done>Group routes are modularized.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Group Service Consolidation & Type Safety Blitz</name>
  <files>
    pickaladder/group/services/group_service.py,
    pickaladder/group/routes/management.py,
    pickaladder/group/routes/membership.py,
    pickaladder/group/routes/sessions.py
  </files>
  <action>
    1. Identify direct Firestore updates in routes (e.g., `resend_invite`, `delete_invite`, `handle_invite`, `join_group`, `leave_group`).
    2. Move these to `GroupService` methods.
    3. Ensure all business logic is encapsulated in `GroupService`, `SessionService`, or `LeaderboardService`.
    4. Add 100% type hint coverage (ANN) to:
       - All files in `pickaladder/group/routes/`
       - All files in `pickaladder/group/services/`
       - `pickaladder/group/models.py`, `pickaladder/group/forms.py`, `pickaladder/group/utils.py`.
    5. Fix existing `C901` (Complexity) issues if they appear in `CODEBASE_AUDIT.md` for these files by splitting functions.
  </action>
  <verify>
    <automated>ruff check pickaladder/group --select ANN && pytest tests/test_group.py</automated>
  </verify>
  <done>Group logic is consolidated in services and type safety is achieved for the Group domain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Group Management | Admin actions (promote, remove, delete) cross boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Elevation of Privilege | Group Management | mitigate | Ensure `GroupService.is_group_admin` check is strictly applied in all management routes. |
| T-22-02-02 | Information Disclosure | Group Trend API | mitigate | Ensure trend data respects group membership/visibility rules. |
| T-22-02-03 | Tampering | Invitations | mitigate | Validate invite tokens and ensure they cannot be reused if `used=True`. |
</threat_model>

<verification>
1. Run `pytest tests/test_group.py` and `tests/test_group_leaderboard.py`.
2. Run `ruff check pickaladder/group --select ANN`.
3. Manually verify group creation, joining, and management flows.
</verification>

<success_criteria>
- `pickaladder/group/routes.py` is removed.
- `pickaladder/group/routes/` contains 5 specialized modules.
- `ruff check pickaladder/group --select ANN` returns no violations.
- Group-related tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/22-architectural-refactor/22-02-SUMMARY.md`
</output>
