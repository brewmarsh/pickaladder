---
phase: 22-architectural-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pickaladder/user/__init__.py
  - pickaladder/user/routes.py
  - pickaladder/user/routes/__init__.py
  - pickaladder/user/routes/profile.py
  - pickaladder/user/routes/friends.py
  - pickaladder/user/routes/api.py
  - pickaladder/user/services/__init__.py
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "User routes are accessible at existing URLs"
    - "User blueprint is registered and functional"
    - "User routes contain zero business logic (all moved to services)"
    - "No Ruff ANN violations in pickaladder/user/"
  artifacts:
    - path: "pickaladder/user/routes/profile.py"
      provides: "Profile and Dashboard routes"
    - path: "pickaladder/user/routes/friends.py"
      provides: "Social and Community routes"
    - path: "pickaladder/user/routes/api.py"
      provides: "User JSON API routes"
  key_links:
    - from: "pickaladder/user/__init__.py"
      to: "pickaladder/user/routes/"
      via: "blueprint registration/imports"
---

<objective>
Modularize User Routes and consolidate business logic into the Service layer to improve maintainability and type safety.

Purpose: Reduce cognitive load by splitting a monolithic 350+ line file and ensure 100% type hint coverage in the User domain.
Output: Refactored User blueprint structure with separate route modules and enhanced services.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CODEBASE_AUDIT.md
@pickaladder/user/routes.py
@pickaladder/user/services/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split User Routes into Modules</name>
  <files>
    pickaladder/user/routes/__init__.py,
    pickaladder/user/routes/profile.py,
    pickaladder/user/routes/friends.py,
    pickaladder/user/routes/api.py,
    pickaladder/user/__init__.py
  </files>
  <behavior>
    - Existing user URLs (/user/settings, /user/dashboard, /user/community, etc.) must remain functional.
    - Blueprints must be correctly wired.
  </behavior>
  <action>
    1. Create `pickaladder/user/routes/` directory.
    2. Create `pickaladder/user/routes/__init__.py`.
    3. Distribute routes from `pickaladder/user/routes.py` into:
       - `profile.py`: settings, reset_avatar, edit_profile, dashboard, view_user (the /<user_id> route).
       - `friends.py`: view_community, users, send_friend_request, accept_request, cancel_request.
       - `api.py`: api_dashboard, create_invite, save_fcm_token, api_search_users.
    4. Move the `CursorPagination` class to a more appropriate place (e.g., `pickaladder/user/utils.py` or keep in `profile.py` if only used there).
    5. Update `pickaladder/user/__init__.py` to import these modules so they attach to the `bp`.
    6. DELETE `pickaladder/user/routes.py` once everything is migrated.
  </action>
  <verify>
    <automated>pytest tests/test_user.py tests/test_user_profile_dupr.py</automated>
  </verify>
  <done>User routes are modularized and tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Service Layer Consolidation & Type Safety Blitz</name>
  <files>
    pickaladder/user/services/__init__.py,
    pickaladder/user/routes/profile.py,
    pickaladder/user/routes/friends.py,
    pickaladder/user/routes/api.py
  </files>
  <action>
    1. Scan new route files for direct Firestore/DB logic.
    2. Move `save_fcm_token` logic (DB update) to `UserService.update_fcm_token`.
    3. Move `api_search_users` formatting logic to a helper or service method if it's too complex for a route.
    4. Systematically add missing type hints (ANN violations) to:
       - All files in `pickaladder/user/routes/`
       - All files in `pickaladder/user/services/`
       - `pickaladder/user/models.py` and `pickaladder/user/forms.py`
    5. Ensure `ruff check pickaladder/user --select ANN` returns zero violations.
  </action>
  <verify>
    <automated>ruff check pickaladder/user --select ANN && pytest tests/test_user.py</automated>
  </verify>
  <done>Business logic is encapsulated in services and 100% type coverage is achieved for the User domain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> User API | External input (search terms, settings, tokens) crosses into the application. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Elevation of Privilege | User Settings | mitigate | Ensure `@login_required` and `g.user` checks are preserved in all split route modules. |
| T-22-01-02 | Information Disclosure | Search API | mitigate | Ensure `UserService.search_users` and the route formatting do not expose private PII. |
| T-22-01-03 | Tampering | FCM Tokens | mitigate | Ensure FCM token updates are strictly tied to the authenticated `g.user.uid`. |
</threat_model>

<verification>
1. Run `pytest tests/test_user.py` to ensure core functionality is preserved.
2. Run `ruff check pickaladder/user --select ANN` to verify type safety.
3. Manually check URLs: `/user/settings`, `/user/dashboard`, `/user/community`.
</verification>

<success_criteria>
- `pickaladder/user/routes.py` is removed.
- `pickaladder/user/routes/` contains modular files < 300 lines each.
- `ruff check pickaladder/user --select ANN` passes.
- All user tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/22-architectural-refactor/22-01-SUMMARY.md`
</output>
