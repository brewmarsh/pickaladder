---
phase: 22-architectural-refactor
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - app.py
  - pickaladder/__init__.py
  - pickaladder/admin/routes.py
  - pickaladder/auth/routes.py
  - pickaladder/auth/decorators.py
  - pickaladder/match/routes.py
  - pickaladder/match/services.py
  - tests/e2e/conftest.py
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "Zero Ruff ANN violations across the entire pickaladder/ directory"
    - "Zero Ruff ANN violations in the tests/ directory"
  artifacts:
    - path: "pickaladder/match/routes.py"
      provides: "Match routes with full type hints"
  key_links: []
---

<objective>
Achieve 100% type hint coverage across the entire project by resolving all remaining Ruff ANN violations.

Purpose: Finalize the type safety goal of the phase, ensuring robust static analysis and improved developer experience.
Output: A fully type-hinted codebase with zero ANN violations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CODEBASE_AUDIT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Resolve Core, Admin, and Auth ANN Violations</name>
  <files>
    app.py,
    pickaladder/__init__.py,
    pickaladder/admin/routes.py,
    pickaladder/auth/routes.py,
    pickaladder/auth/decorators.py,
    pickaladder/auth/forms.py,
    pickaladder/error_handlers.py,
    pickaladder/errors.py,
    pickaladder/utils.py
  </files>
  <action>
    1. Run `ruff check . --select ANN` to get current violations.
    2. Add missing type hints to function arguments and return types in the listed core files.
    3. Use `Any` sparingly, preferring specific types or `flask.Response` where appropriate.
  </action>
  <verify>
    <automated>ruff check pickaladder/admin pickaladder/auth --select ANN</automated>
  </verify>
  <done>Core, Admin, and Auth modules have 100% type coverage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Resolve Match Domain ANN Violations</name>
  <files>
    pickaladder/match/routes.py,
    pickaladder/match/forms.py,
    pickaladder/match/services.py
  </files>
  <action>
    1. Resolve all ANN violations in the match blueprint.
    2. Note: `pickaladder/match/routes.py` is also large (539 lines). While not explicitly requested for modularization, ensure it at least follows the 300-line guideline if easy to split, otherwise focus on type hints.
  </action>
  <verify>
    <automated>ruff check pickaladder/match --select ANN</automated>
  </verify>
  <done>Match domain has 100% type coverage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Resolve Test and Infrastructure ANN Violations</name>
  <files>
    tests/e2e/conftest.py,
    tests/conftest.py,
    tests/test_admin.py,
    tests/test_auth.py,
    tests/test_group.py,
    tests/test_match.py,
    tests/test_user.py
  </files>
  <action>
    1. Resolve the 80+ violations in `tests/e2e/conftest.py`.
    2. Resolve all remaining ANN violations in the `tests/` directory.
    3. Final pass: `ruff check . --select ANN` must return 0 violations.
  </action>
  <verify>
    <automated>ruff check . --select ANN</automated>
  </verify>
  <done>Entire project has 100% type hint coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A - This is a type-safety refactor.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | N/A | Type Hints | accept | Type hints do not introduce security vulnerabilities but improve code clarity and catch bugs during development. |
</threat_model>

<verification>
1. Run `ruff check . --select ANN` to ensure zero violations.
2. Run all tests `pytest` to ensure no regressions were introduced by type hint additions (e.g., circular imports).
</verification>

<success_criteria>
- `ruff check . --select ANN` passes with zero violations.
- All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/22-architectural-refactor/22-03-SUMMARY.md`
</output>
