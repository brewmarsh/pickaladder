---
phase: 21-data-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "pickaladder/core/pagination.py",
  "pickaladder/user/services/core.py",
  "pickaladder/user/routes.py",
  "pickaladder/match/services/query.py",
  "pickaladder/group/services/group_service.py",
  "pickaladder/group/services/leaderboard.py",
  "pickaladder/group/repository.py",
  "firestore.indexes.json"
]
autonomous: true
requirements: [SCALE-01, SCALE-02]
must_haves:
  truths:
    - "User can fetch subsequent pages of users using a cursor"
    - "Match history uses a standardized cursor-based response format"
    - "Group Hub fetches fewer than 50% of the previous document count for large groups"
  artifacts:
    - path: "pickaladder/core/pagination.py"
      provides: "FirestorePaginator utility"
    - path: "tests/test_pagination_utils.py"
      provides: "Automated verification for pagination logic"
  key_links:
    - from: "pickaladder/user/routes.py"
      to: "pickaladder/core/pagination.py"
      via: "import FirestorePaginator"
    - from: "pickaladder/group/services/group_service.py"
      to: "pickaladder/group/services/leaderboard.py"
      via: "optimized function arguments"
---

<objective>
Implement a reusable cursor-based pagination utility for Firestore and apply it to high-volume lists (Users, Matches). Optimize core group views to resolve N+1 query patterns and reduce redundant document fetches.

Purpose: Improve application scalability and reduce Firestore read costs as data volume grows.
Output: Standardized pagination utility, paginated routes, and optimized service layer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pickaladder/group/services/group_service.py
@pickaladder/group/services/leaderboard.py
@pickaladder/match/services/query.py
@pickaladder/user/services/core.py
@firestore.indexes.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Standardized Pagination Utility</name>
  <files>pickaladder/core/pagination.py, tests/test_pagination_utils.py</files>
  <behavior>
    - Given a Firestore query, it should return a page of items and a 'next_cursor'.
    - It should support both Document IDs and Field Values as cursors.
    - It should handle empty results gracefully.
    - It should allow setting a maximum limit to prevent DoS.
  </behavior>
  <action>
    - Create `pickaladder/core/pagination.py` containing a `FirestorePaginator` class.
    - Implement `paginate(query, limit, cursor_id=None)` method.
    - The paginator should use `query.start_after(last_doc)` for cursor-based fetching.
    - Create `tests/test_pagination_utils.py` using `mockfirestore` to verify the logic.
  </action>
  <verify>
    <automated>pytest tests/test_pagination_utils.py</automated>
  </verify>
  <done>Reusable pagination utility implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Pagination to User and Member Lists</name>
  <files>pickaladder/user/services/core.py, pickaladder/user/routes.py, pickaladder/group/repository.py, firestore.indexes.json</files>
  <action>
    - Refactor `UserService.search_users` in `pickaladder/user/services/core.py` to accept `limit` and `cursor`.
    - Integrate `FirestorePaginator` into `search_users`.
    - Update `pickaladder/user/routes.py` (users route) to read pagination params from request and pass to service.
    - Replace `MockPagination` in `user/routes.py` with real pagination data.
    - Refactor `GroupRepository.get_group_members` in `pickaladder/group/repository.py` to support slicing for pagination.
    - Add required composite indexes to `firestore.indexes.json` if ordering is applied during search (e.g., username + createdAt).
  </action>
  <verify>
    <automated>pytest tests/test_user.py</automated>
  </verify>
  <done>Global user list and group member lists support cursor-based pagination.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Match History to use Standard Paginator</name>
  <files>pickaladder/match/services/query.py, pickaladder/match/routes.py</files>
  <action>
    - Refactor `MatchQueryService.get_matches_for_user` in `pickaladder/match/services/query.py` to use `FirestorePaginator`.
    - Standardize the return format to `(items, next_cursor)`.
    - Ensure `match/routes.py` (`get_match_history`) correctly passes parameters and returns the standardized response.
  </action>
  <verify>
    <automated>pytest tests/test_match.py</automated>
  </verify>
  <done>Match history uses the standardized pagination utility.</done>
</task>

<task type="auto">
  <name>Task 4: Resolve N+1 Patterns in Group Details & Leaderboard</name>
  <files>pickaladder/group/services/group_service.py, pickaladder/group/services/leaderboard.py</files>
  <action>
    - Optimize `get_group_leaderboard` in `pickaladder/group/services/leaderboard.py` to accept optional `member_docs` and `cached_matches`.
    - In `GroupService.get_group_details` (`pickaladder/group/services/group_service.py`), avoid re-fetching member documents that were already fetched via `GroupRepository`.
    - Pass the already fetched member data to `get_group_leaderboard` to eliminate redundant `db.get_all` calls.
    - Refactor `get_group_leaderboard` to avoid fetching the group document if its data is already provided.
    - Ensure `GroupService.get_user_groups` is optimized if possible (caching or batching leaderboard lookups).
  </action>
  <verify>
    <automated>pytest tests/test_group.py</automated>
  </verify>
  <done>N+1 query patterns resolved in Group Hub and Leaderboard views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Request parameters (limit, cursor) cross this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Denial of Service | Pagination Utility | mitigate | Enforce a strict maximum limit (e.g., 100 items) regardless of user input. |
| T-21-02 | Information Disclosure | Firestore Cursor | mitigate | Validate that the cursor (document ID) exists and is accessible before using it in a query. |
| T-21-03 | Tampering | Pagination Params | mitigate | Type-check and sanitize 'limit' and 'cursor' inputs using standard patterns. |
</threat_model>

<verification>
1. Run `pytest tests/test_pagination_utils.py` to verify the foundation.
2. Manually verify `GET /users?limit=5` returns 5 users and a cursor.
3. Manually verify `GET /users?limit=5&cursor=[ID]` returns the next set of users.
4. Run `pytest tests/test_group.py` and check logs/counts to ensure document fetches are reduced (can be verified with mocks).
</verification>

<success_criteria>
- `FirestorePaginator` is implemented and used in at least 3 distinct services.
- User list and Match history support infinite scroll/pagination via API.
- Group details view performs significantly fewer Firestore reads for large groups.
</success_criteria>

<output>
After completion, create `.planning/phases/21-data-access/21-01-SUMMARY.md`
</output>
