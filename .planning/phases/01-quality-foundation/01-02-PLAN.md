---
phase: 01-quality-foundation
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified: [pickaladder/admin/routes.py, pickaladder/config.py, pickaladder/__init__.py, pyproject.toml, .github/workflows/ci.yaml]
autonomous: true
requirements: [AUDIT-01, AUDIT-02, QUAL-01, QUAL-03]
must_haves:
  truths:
    - "admin.generate_users route requires admin privileges"
    - "Environment variables are consolidated into a central Config class"
    - "mypy is configured in pyproject.toml and passing"
    - "CI/CD automatically verifies linting, types, and tests"
  artifacts:
    - path: "pickaladder/admin/routes.py"
      provides: "Secured administrative routes"
    - path: "pickaladder/config.py"
      provides: "Centralized configuration management"
    - path: "pyproject.toml"
      provides: "mypy configuration"
  key_links:
    - from: "pickaladder/__init__.py"
      to: "pickaladder/config.py"
      via: "app.config.from_object"
    - from: ".github/workflows/ci.yaml"
      to: "mypy"
      via: "typing job"
---

<objective>
Fix critical security vulnerability in admin routes, implement centralized configuration, and establish static type checking with CI/CD integration.

Purpose: Harden the application against unauthorized data injection and improve codebase maintainability.
Output: Secured admin routes, new config module, mypy configuration, and verified CI/CD pipeline.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/audit/AUDIT_REPORT.md
@.planning/phases/01-quality-foundation/01-01-SUMMARY.md
@pickaladder/admin/routes.py
@pickaladder/__init__.py
@pyproject.toml
@.github/workflows/ci.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secure admin.generate_users endpoint</name>
  <files>pickaladder/admin/routes.py</files>
  <action>
    Fix the security vulnerability identified in AUDIT-01.
    - Add `@login_required(admin_required=True)` decorator to the `generate_users` function in `pickaladder/admin/routes.py`.
    - Ensure all other admin routes in the same file are properly decorated (audit revealed `generate_users` was the only one missing it).
  </action>
  <verify>
    <automated>grep -A 1 "generate_users" pickaladder/admin/routes.py | grep -q "@login_required(admin_required=True)"</automated>
  </verify>
  <done>Administrative user generation is now protected by authentication and authorization.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize Configuration (AUDIT-02)</name>
  <files>pickaladder/config.py, pickaladder/__init__.py</files>
  <action>
    Consolidate environment variable usage into a central `Config` class.
    - Create `pickaladder/config.py` with a `Config` class containing all settings currently in `pickaladder/__init__.py`'s `_load_config`.
    - Handle defaults and type conversions (e.g., `int` for ports, `bool` for TLS/SSL).
    - Remove `_load_config` logic from `pickaladder/__init__.py` and replace with `app.config.from_object(Config)`.
    - Ensure `SECRET_KEY` defaults to "dev" only in non-production environments.
    - Remove sensitive debug logging of mail passwords in `_configure_mail_logging`.
  </action>
  <verify>
    <automated>python -c "from pickaladder.config import Config; print(Config.MAIL_SERVER)"</automated>
  </verify>
  <done>Application configuration is centralized and decoupled from the app initialization logic.</done>
</task>

<task type="auto">
  <name>Task 3: Configure mypy and fix initial issues (QUAL-01)</name>
  <files>pyproject.toml</files>
  <action>
    Establish static type checking standards.
    - Add `[tool.mypy]` section to `pyproject.toml`.
    - Configure `mypy` with `strict = false` initially but enable key flags: `warn_return_any`, `warn_unused_configs`, `disallow_untyped_defs = false`, `check_untyped_defs = true`.
    - Run `mypy .` and fix "low-hanging fruit" type errors, specifically those related to the new `Config` class and admin routes.
    - Use `ignore_missing_imports` for libraries without stubs (e.g., `firebase_admin`).
  </action>
  <verify>
    <automated>uv run python -m mypy .</automated>
  </verify>
  <done>mypy is configured and running as part of the local development workflow.</done>
</task>

<task type="auto">
  <name>Task 4: Verify CI/CD Pipeline (QUAL-03)</name>
  <files>.github/workflows/ci.yaml</files>
  <action>
    Ensure the CI/CD pipeline correctly enforces the new quality standards.
    - Review `.github/workflows/ci.yaml` to confirm the `typing` job is using the project's `mypy` configuration (no extra flags that bypass `pyproject.toml`).
    - Confirm `test` job passes with the new configuration system.
    - If necessary, update the workflow to ensure it fails on any type errors or linting violations.
  </action>
  <verify>
    <automated>grep -q "mypy" .github/workflows/ci.yaml</automated>
  </verify>
  <done>CI/CD pipeline automatically verifies code quality on every push to main or beta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin -> generate_users | Authenticated admin access only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Elevation of Privilege | admin.generate_users | mitigate | Enforce admin_required=True decorator |
| T-01-02-02 | Information Disclosure | pickaladder/config.py | mitigate | Ensure sensitive env vars are not hardcoded or logged |
</threat_model>

<success_criteria>
1. `generate_users` route is protected by `@login_required(admin_required=True)`.
2. `pickaladder/config.py` exists and is used by `create_app`.
3. `uv run python -m mypy .` passes with the new configuration.
4. CI/CD workflow contains a passing `typing` job.
</success_criteria>

<output>
After completion, create .planning/phases/01-quality-foundation/01-02-SUMMARY.md
</output>
