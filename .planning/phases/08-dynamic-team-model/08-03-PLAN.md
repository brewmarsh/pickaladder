---
phase: 08
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified: [pickaladder/match/templates/record_match.html, pickaladder/static/js/match_recording.js]
autonomous: false
requirements: [DYNAMIC-02]
must_haves:
  truths:
    - "Record Match form has a toggle for 'Manual Pair' vs 'Saved Team'"
    - "Selecting a Saved Team populates a participant picker with its roster"
    - "Recording a match from a Saved Team correctly passes the Named Team ID to the backend"
  artifacts:
    - path: "pickaladder/match/templates/record_match.html"
      provides: "Updated UI for team selection"
    - path: "pickaladder/static/js/match_recording.js"
      provides: "Client-side logic for roster picking"
  key_links:
    - from: "record_match.html"
      to: "match_recording.js"
      via: "event listeners"
---

<objective>
Implement the UI components for selecting Named Teams and picking participants from their rosters during the match recording process.

Purpose: Provide a seamless user interface for dynamic team matches.
Output: Updated match recording form with team/roster selection logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dynamic-team-model/RESEARCH.md
@.planning/phases/08-dynamic-team-model/08-02-SUMMARY.md
@pickaladder/match/templates/record_match.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Match Recording Template</name>
  <files>pickaladder/match/templates/record_match.html</files>
  <action>
    - Add a "Selection Mode" toggle (Manual Pair vs. Saved Team) for each side of the match.
    - Add hidden inputs for `named_team_1_id` and `named_team_2_id`.
    - Include a search/dropdown container for Named Teams.
    - Add a "Participant Picker" container that will be dynamically populated from the roster.
  </action>
  <verify>
    <automated>grep -q "Selection Mode" pickaladder/match/templates/record_match.html</automated>
  </verify>
  <done>UI elements for Named Team selection are present in the template.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Team & Roster Selection Logic</name>
  <files>pickaladder/static/js/match_recording.js</files>
  <action>
    - Implement JS logic to fetch and display Named Teams the user belongs to.
    - When a team is selected, fetch its roster and populate the Participant Picker.
    - Update the participant selection logic to ensure exactly 2 people are chosen from the roster.
    - Ensure the form submission includes the Named Team ID and the selected participant IDs.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create tests/e2e/test_team_selection.py first</automated>
  </verify>
  <done>Users can record matches by selecting a Named Team and picking participants from its roster.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Named Team selection and Roster participant picking in the Record Match form.</what-built>
  <how-to-verify>
    1. Navigate to Record Match.
    2. Toggle "Saved Team" for Side 1.
    3. Select a Named Team (create one via admin or script if needed).
    4. Verify the roster is displayed.
    5. Pick two members from the roster.
    6. Record match and verify the match document in Firestore (or Match History) reflects the team and players.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Backend API | Match submission payload |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | Participant Picker | mitigate | Ensure roster data is only fetched for teams the user is authorized to view. |
</threat_model>

<verification>
End-to-end verification of the match recording flow using a Named Team.
</verification>

<success_criteria>
- Users can choose between manual entry and saved teams.
- Selecting a team filters the available participants to its roster.
- Match recording works correctly with the new UI flow.
</success_criteria>

<output>
After completion, create .planning/phases/08-dynamic-team-model/08-03-SUMMARY.md
</output>
