---
phase: 08-dynamic-team-model
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [
  pickaladder/match/models.py,
  pickaladder/match/services/command.py,
  pickaladder/match/services/match_stats_updater.py,
  tests/test_match_models_update_v8.py
]
autonomous: true
requirements: [DYNAMIC-02, DYNAMIC-03]

must_haves:
  truths:
    - "Match recording supports linking to both a Pairing and an optional Named Team."
    - "Team stats (ELO, wins/losses) are updated for both Pairings and Named Teams in a single transaction."
    - "MatchStatsUpdater correctly handles namedTeamId field updates."
  artifacts:
    - path: "tests/test_match_models_update_v8.py"
      provides: "Verification of dual-team stat tracking"
---

<objective>
Refactor the match recording service and stat updater to support dual tracking (Pairing + Named Team) and implement ELO calculation for Named Teams.

Purpose: Ensures performance metrics are aggregated correctly at both duo and team levels.
Output: Session-aware MatchCommandService and dual-tracking MatchStatsUpdater.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dynamic-team-model/RESEARCH.md
@.planning/phases/08-dynamic-team-model/VALIDATION.md
@pickaladder/match/models.py
@pickaladder/match/services/command.py
@pickaladder/match/services/match_stats_updater.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Match Models and Submission Logic</name>
  <files>pickaladder/match/models.py, tests/test_match_models_update_v8.py</files>
  <action>
    1. Update `MatchSubmission` and `Match` models to include optional `namedTeam1Id` and `namedTeam2Id`.
    2. Create `tests/test_match_models_update_v8.py` to verify model serialization/deserialization.
  </action>
  <verify>
    <automated>uv run pytest tests/test_match_models_update_v8.py</automated>
  </verify>
  <done>Match models support dual-team linkage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dual-Team Stat Aggregation</name>
  <files>pickaladder/match/services/command.py, pickaladder/match/services/match_stats_updater.py</files>
  <action>
    1. Modify `MatchCommandService.record_match`:
       - Accept `namedTeam1Id` and `namedTeam2Id` from input.
       - Pass these IDs down to the stat updater.
    2. Update `MatchStatsUpdater.apply_stats_delta`:
       - Calculate and apply ELO updates for both the specific Pairing AND the associated Named Team.
       - Ensure all updates are included in the Firestore batch.
  </action>
  <verify>
    <automated>uv run pytest tests/test_match_transaction.py</automated>
  </verify>
  <done>Stats are correctly aggregated for both pairings and named teams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Match Stats -> Team Entity | Consistency of aggregate ELO. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Denial of Service | Stat Updater | mitigate | Use Firestore Batches and limit the number of participants per roster to prevent massive multi-doc transaction failures. |
</threat_model>

<success_criteria>
1. Matches link to both specific Pairings and aggregate Named Teams.
2. Named Teams have their own ELO and win/loss records.
3. Stat updates are atomic and verified by integration tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dynamic-team-model/08-02-SUMMARY.md`
</output>
