---
phase: 08-dynamic-team-model
plan: 01
type: execute
wave: 1
depends_on: [07-03]
files_modified: [
  pickaladder/teams/repository.py, 
  pickaladder/teams/services.py,
  pickaladder/teams/routes.py,
  pickaladder/teams/forms.py,
  pickaladder/templates/team/create.html,
  tests/test_team_repository_v8.py,
  tests/e2e/test_dynamic_teams.py
]
autonomous: true
requirements: [DYNAMIC-01]

must_haves:
  truths:
    - "TeamRepository distinguishes between 'pairing' and 'named' teams."
    - "Named teams support rosters (memberIds array)."
    - "Users can create named teams via a dedicated route and UI."
  artifacts:
    - path: "pickaladder/templates/team/create.html"
      provides: "UI for named team creation"
    - path: "tests/test_team_repository_v8.py"
      provides: "Unit tests for roster-based repository logic"
---

<objective>
Update the data model and repository to support dynamic rosters (Named Teams) and implement the UI/API for creating these teams.

Purpose: Enables users to form named groups for aggregate performance tracking.
Output: Enhanced TeamRepository, team creation route, and management UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dynamic-team-model/RESEARCH.md
@.planning/phases/08-dynamic-team-model/VALIDATION.md
@pickaladder/teams/repository.py
@pickaladder/teams/services.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Initialize Test Scaffolding (Wave 0)</name>
  <files>tests/test_team_repository_v8.py, tests/e2e/test_dynamic_teams.py</files>
  <action>
    Create placeholder test files for the new dynamic team features.
    - `tests/test_team_repository_v8.py` for unit testing the new repository methods.
    - `tests/e2e/test_dynamic_teams.py` for verifying the creation flow.
  </action>
  <verify>
    <automated>ls tests/test_team_repository_v8.py tests/e2e/test_dynamic_teams.py</automated>
  </verify>
  <done>Test scaffolding is initialized.</done>
</task>

<task type="auto">
  <name>Task 1: Update Repository and Data Model</name>
  <files>pickaladder/teams/repository.py, pickaladder/teams/services.py</files>
  <action>
    1. Update `TeamRepository`:
       - Add `type` (pairing/named) and `memberIds` to creation logic.
       - Refactor `get_or_create_team` to strictly use `type="pairing"`.
       - Add `create_named_team(db, name, creator_id, member_ids)` method.
       - Add `get_user_named_teams(db, user_id)` using `array-contains` query.
    2. Update `TeamService` to wrap these new repository methods.
  </action>
  <verify>
    <automated>uv run pytest tests/test_team_repository_v8.py</automated>
  </verify>
  <done>Repository layer supports dynamic rosters and named teams.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Team Creation UI and Routes</name>
  <files>pickaladder/teams/routes.py, pickaladder/teams/forms.py, pickaladder/templates/team/create.html</files>
  <action>
    1. Create `TeamForm` in `pickaladder/teams/forms.py` with name and member selection.
    2. Add `/teams/create` route in `pickaladder/teams/routes.py`.
    3. Create `pickaladder/templates/team/create.html` with a high-contrast form for team creation.
  </action>
  <verify>
    <automated>uv run pytest tests/e2e/test_dynamic_teams.py</automated>
  </verify>
  <done>Users can create named teams via the web interface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Team Route | Input validation for roster size and membership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | Team Roster | mitigate | Verify current user is a group admin or owner before allowing team creation in that context. |
</threat_model>

<success_criteria>
1. Named teams can be created and persisted in Firestore with `type: "named"`.
2. `TeamRepository.get_or_create_team` remains backward compatible for pairings.
3. Creation UI is functional and follows high-contrast standards.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dynamic-team-model/08-01-SUMMARY.md`
</output>
