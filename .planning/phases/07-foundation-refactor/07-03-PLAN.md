---
phase: 07-foundation-refactor
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [pickaladder/teams/models.py, pickaladder/teams/repository.py, pickaladder/group/services/group_service.py, pickaladder/group/repository.py]
autonomous: true
requirements: [REFACTOR-02]

must_haves:
  truths:
    - "Team creation logic is centralized in TeamRepository.get_or_create_team."
    - "Group validation logic is centralized in GroupRepository."
    - "Deprecated static creation methods in models.py are removed or marked as deprecated."
  artifacts:
    - path: "pickaladder/teams/repository.py"
      provides: "Centralized get_or_create_team logic"
    - path: "pickaladder/group/repository.py"
      provides: "Centralized group validation"
  key_links:
    - from: "TeamService"
      to: "TeamRepository.get_or_create_team"
      via: "primary creation path"
---

<objective>
Consolidate and centralize the creation and validation logic for Groups and Teams within their respective repositories and services, and clean up deprecated models/static methods.

Purpose: Ensures that core entities are created and validated consistently, reducing duplication and potential for bugs (like duplicate teams).
Output: Centralized creation logic, improved validation, and cleaner models.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-refactor/RESEARCH.md
@pickaladder/teams/models.py
@pickaladder/teams/repository.py
@pickaladder/group/services/group_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate Team Creation Logic</name>
  <files>pickaladder/teams/models.py, pickaladder/teams/repository.py, pickaladder/teams/services.py</files>
  <action>
    1. Move the logic from `pickaladder/teams/models.py:create_team_document` into `TeamRepository.get_or_create_team(cls, db, member_ids)`.
    2. Ensure `get_or_create_team` handles:
       - Alphabetical sorting of member IDs.
       - Checking for existing teams with the same member IDs.
       - Generating the team name consistently.
       - Using `BaseRepository.create` for auto-timestamps.
    3. Update `TeamService` to use `TeamRepository.get_or_create_team`.
    4. Delete (or deprecate with a clear message) `create_team_document` in `pickaladder/teams/models.py`.
  </action>
  <verify>
    <automated>pytest tests/test_team_service.py</automated>
  </verify>
  <done>Team creation logic is centralized in the Repository layer; duplicates are prevented by design.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Group Creation and Validation</name>
  <files>pickaladder/group/services/group_service.py, pickaladder/group/repository.py</files>
  <action>
    1. Extract group validation logic (e.g., checking if a group name is unique, member limits) from `GroupService` into `GroupRepository.validate(cls, data)`.
    2. Centralize `get_or_create_group` (if applicable) or ensure `create` in `GroupRepository` performs necessary pre-checks.
    3. Update `GroupService.create_group` to use these centralized repository methods.
  </action>
  <verify>
    <automated>pytest tests/test_group.py</automated>
  </verify>
  <done>Group creation and validation logic is centralized and consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Validation | Internal logic ensuring data integrity before persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Deny of Service | TeamRepository | mitigate | Implement index-backed queries in `get_or_create_team` to prevent slow table scans when checking for existing teams. |
| T-07-06 | Tampering | GroupRepository | mitigate | Ensure validation logic enforces business rules (e.g., maximum members) at the repository level as a final gate. |
</threat_model>

<verification>
1. Run `tests/test_team_service.py` specifically focusing on team creation and member sorting.
2. Run `tests/test_group.py` to verify group creation validation.
3. Verify that `create_team_document` is no longer used project-wide (grep search).
</verification>

<success_criteria>
- TeamRepository.get_or_create_team is the single source of truth for team creation.
- Team members are always sorted, preventing duplicate teams for the same pair.
- Group validation logic is encapsulated within the Repository layer.
- All tests pass with 100% coverage for creation logic.
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-refactor/07-03-SUMMARY.md`
</output>
