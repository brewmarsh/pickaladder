---
phase: 07-foundation-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  pickaladder/base/repository.py, 
  pickaladder/core/types.py, 
  scripts/migrate_timestamps_v7.py, 
  pickaladder/teams/models.py, 
  pickaladder/group/services/group_service.py,
  tests/test_referral.py
]
autonomous: true
requirements: [REFACTOR-01, REFACTOR-03]

must_haves:
  truths:
    - "BaseRepository provides _enrich, auto-timestamps, and get_all batch support."
    - "FirestoreDocument TypedDict uses createdAt and updatedAt."
    - "Existing teams and group_invites documents use createdAt in Firestore."
  artifacts:
    - path: "pickaladder/base/repository.py"
      provides: "Expanded BaseRepository"
    - path: "scripts/migrate_timestamps_v7.py"
      provides: "Data migration for schema alignment"
  key_links:
    - from: "BaseRepository.create"
      to: "firestore.SERVER_TIMESTAMP"
      via: "automatic field injection"
---

<objective>
Expand the BaseRepository to support standardized CRUD operations with automatic timestamping and batch fetching, and migrate existing Firestore data to use the new `createdAt`/`updatedAt` camelCase schema.

Purpose: Establishes the technical foundation for standardized data access and ensures schema consistency across the codebase.
Output: Updated BaseRepository, updated core types, and migrated Firestore data.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-refactor/RESEARCH.md
@.planning/phases/07-foundation-refactor/VALIDATION.md
@pickaladder/base/repository.py
@pickaladder/core/types.py
@pickaladder/teams/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand BaseRepository and Update Core Types</name>
  <files>pickaladder/base/repository.py, pickaladder/core/types.py</files>
  <action>
    1. Modify `pickaladder/base/repository.py`:
       - Add `_enrich(cls, doc_snap)` method to inject document ID into data and handle existence checks.
       - Update `create(cls, db, data)` to automatically inject `createdAt = firestore.SERVER_TIMESTAMP`.
       - Update `update(cls, db, doc_id, data)` to automatically inject `updatedAt = firestore.SERVER_TIMESTAMP`.
       - Add `get_all(cls, db, refs)` method using `db.get_all()` for efficient batch fetching.
       - Ensure `get_by_id` uses `_enrich`.
    2. Modify `pickaladder/core/types.py`:
       - Rename `created_at` to `createdAt` in `_FirestoreDocumentBase`.
       - Rename `updated_at` to `updatedAt` in `FirestoreDocument`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_match_transaction.py</automated>
  </verify>
  <done>BaseRepository supports auto-timestamps and batch fetching; FirestoreDocument types use camelCase.</done>
</task>

<task type="auto">
  <name>Task 2: Create and Run Schema Migration Script</name>
  <files>scripts/migrate_timestamps_v7.py, tests/test_referral.py</files>
  <action>
    1. Create `scripts/migrate_timestamps_v7.py` to:
       - Iterate through all documents in `teams` and `group_invites` collections.
       - Rename `created_at` field to `createdAt` if it exists.
       - Handle batch writes (max 500 per batch) for efficiency and safety.
    2. Run the script using `python scripts/migrate_timestamps_v7.py`.
    3. Update `pickaladder/teams/models.py`, `pickaladder/group/services/group_service.py`, and `tests/test_referral.py` to use `createdAt` instead of `created_at`.
  </action>
  <verify>
    <automated>uv run ruff check scripts/migrate_timestamps_v7.py && uv run pytest tests/test_referral.py</automated>
  </verify>
  <done>Existing data in Firestore is migrated to camelCase; code references are updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Firestore | Internal data storage; timestamps are injected server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | BaseRepository | mitigate | Use server-side timestamps (`SERVER_TIMESTAMP`) for createdAt/updatedAt to prevent client-side clock manipulation. |
| T-07-02 | Information Disclosure | Migration Script | mitigate | Ensure migration script only runs with appropriate service account permissions and logs summary results without PII. |
</threat_model>

<verification>
1. Verify `BaseRepository` methods via existing tests (e.g., `tests/test_match_transaction.py` which uses `MatchCommandService`).
2. Manual check of Firestore emulator/console to confirm `teams` documents have `createdAt`.
3. Run `uv run mypy pickaladder` to ensure type changes are consistent.
</verification>

<success_criteria>
- BaseRepository automatically adds timestamps on creation and update.
- core/types.py uses camelCase for timestamps.
- Firestore collections 'teams' and 'group_invites' use 'createdAt' field.
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-refactor/07-01-SUMMARY.md`
</output>
