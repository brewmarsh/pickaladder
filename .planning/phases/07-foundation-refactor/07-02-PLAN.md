---
phase: 07-foundation-refactor
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [pickaladder/group/repository.py, pickaladder/teams/repository.py, pickaladder/group/services/group_service.py, pickaladder/teams/services.py]
autonomous: true
requirements: [REFACTOR-03]

must_haves:
  truths:
    - "GroupRepository and TeamRepository exist and inherit from BaseRepository."
    - "GroupService uses GroupRepository for data access."
    - "TeamService uses TeamRepository for data access."
  artifacts:
    - path: "pickaladder/group/repository.py"
      provides: "Standardized Group data access"
    - path: "pickaladder/teams/repository.py"
      provides: "Standardized Team data access"
  key_links:
    - from: "GroupService"
      to: "GroupRepository"
      via: "dependency injection or direct calls"
---

<objective>
Implement specialized repositories for Groups and Teams and refactor the corresponding services to use these repositories for all Firestore interactions.

Purpose: Decouples business logic from data access and standardizes Firestore interactions across the core entities.
Output: New GroupRepository and TeamRepository classes, refactored services.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-refactor/RESEARCH.md
@pickaladder/base/repository.py
@pickaladder/group/services/group_service.py
@pickaladder/teams/services.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GroupRepository and TeamRepository</name>
  <files>pickaladder/group/repository.py, pickaladder/teams/repository.py</files>
  <action>
    1. Create `pickaladder/group/repository.py`:
       - Define `GroupRepository(BaseRepository)` with `COLLECTION_NAME = "groups"`.
       - Implement `get_user_groups(cls, db, user_id)` helper.
    2. Create `pickaladder/teams/repository.py`:
       - Define `TeamRepository(BaseRepository)` with `COLLECTION_NAME = "teams"`.
       - Implement `get_team_by_members(cls, db, member_ids)` helper (ensuring ID sorting).
  </action>
  <verify>
    <automated>pytest tests/test_group.py tests/test_team_service.py</automated>
  </verify>
  <done>GroupRepository and TeamRepository are implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Services to Use Repositories</name>
  <files>pickaladder/group/services/group_service.py, pickaladder/teams/services.py</files>
  <action>
    1. Modify `pickaladder/group/services/group_service.py`:
       - Replace direct `db.collection("groups")` calls with `GroupRepository` methods.
       - Use `GroupRepository.create` for new groups.
    2. Modify `pickaladder/teams/services.py`:
       - Replace direct `db.collection("teams")` calls with `TeamRepository` methods.
       - Use `TeamRepository.create` for new teams.
  </action>
  <verify>
    <automated>pytest tests/test_group.py tests/test_team_service.py</automated>
  </verify>
  <done>GroupService and TeamService are refactored to use the new repository layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Repository | Business logic layer interacting with data access layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Elevation of Privilege | GroupRepository | mitigate | Ensure that any user-scoped queries (like get_user_groups) strictly filter by the provided user_id. |
| T-07-04 | Information Disclosure | TeamRepository | mitigate | Prevent leaking sensitive member data in repository returns; services should filter/transform output. |
</threat_model>

<verification>
1. Run existing group and team service tests to ensure no regressions.
2. Verify that services no longer contain raw `db.collection` strings for 'groups' and 'teams'.
</verification>

<success_criteria>
- GroupRepository and TeamRepository handle all CRUD for their respective collections.
- Services use Repositories instead of direct Firestore calls.
- All existing tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-refactor/07-02-SUMMARY.md`
</output>
