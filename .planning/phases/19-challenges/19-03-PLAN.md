---
phase: 19
plan: 03
type: execute
wave: 1
depends_on: ["19-01", "19-02"]
files_modified: [
  "pickaladder/match/services/challenge_service.py",
  "pickaladder/static/js/notifications.js",
  "pickaladder/static/js/challenges.js",
  "pickaladder/templates/match/_issue_challenge_modal.html",
  "pickaladder/templates/match/_challenge_hub.html"
]
autonomous: true
requirements: ["CHAL-01", "CHAL-02", "CHAL-03"]

must_haves:
  truths:
    - "User receives a real-time notification (toast) when they receive a new challenge."
    - "User receives a notification when their challenge is accepted, declined, or resolved."
    - "Challenge issuance fails if wager > 50 credits."
    - "Challenge issuance fails if user already has 3 pending/accepted challenges."
    - "Challenges expire and show as 'expired' after 48 hours."
    - "Navbar credit balance updates in real-time across tabs when Firestore data changes."
  artifacts:
    - path: "pickaladder/match/services/challenge_service.py"
      provides: "Backend enforcement of competitive limits and shorter expiration."
    - path: "pickaladder/static/js/notifications.js"
      provides: "Global Firestore listeners for challenges and user credits."
    - path: "pickaladder/static/js/challenges.js"
      provides: "Real-time Hub UI synchronization without page reloads."
  key_links:
    - from: "pickaladder/static/js/notifications.js"
      to: "firestore.collection('challenges')"
      via: "onSnapshot listener"
    - from: "pickaladder/static/js/notifications.js"
      to: "firestore.doc('users/{uid}')"
      via: "onSnapshot listener for credit balance"
---

<objective>
Integrate real-time notifications for the challenge system and enforce competitive lifecycle rules (wager limits, active caps, and shorter expiration). 

Purpose: Drive engagement via immediate feedback and maintain a healthy virtual economy.
Output: Real-time notification system, safety-enforced backend, and a reactive Challenge Hub UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-challenges/19-01-SUMMARY.md
@.planning/phases/19-challenges/19-02-SUMMARY.md

<interfaces>
From pickaladder/match/services/challenge_service.py:
```python
class ChallengeService:
    @classmethod
    def issue_challenge(cls, db: Client, challenger_id: str, challenged_id: str, wager: int) -> str: ...
    @classmethod
    def accept_challenge(cls, db: Client, challenge_id: str, user_id: str) -> None: ...
```

From pickaladder/static/js/notifications.js:
```javascript
// Exposes showToast(message, type)
function showToast(message, type) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce Competitive Limits & Expiration</name>
  <files>pickaladder/match/services/challenge_service.py, tests/test_challenge_safety.py</files>
  <behavior>
    - issue_challenge: raise ValueError if wager > 50.
    - issue_challenge: raise ValueError if user has >= 3 active (pending/accepted) challenges.
    - issue_challenge: set expires_at to 48 hours from now (instead of 7 days).
    - accept_challenge: ensure it still checks expiration before processing.
  </behavior>
  <action>
    Modify `ChallengeService.issue_challenge`:
    1. Define `MAX_WAGER = 50`.
    2. Define `MAX_ACTIVE_CHALLENGES = 3`.
    3. Before the transaction, query `challenges` where `challenger_id == challenger_id` and `status` in `['pending', 'accepted']`.
    4. Raise `ValueError` if limits exceeded.
    5. Update `expires_at` calculation to `timedelta(hours=48)`.
    
    Update `tests/test_challenge_safety.py` to verify these limits.
  </action>
  <verify>
    <automated>pytest tests/test_challenge_safety.py</automated>
  </verify>
  <done>Backend enforces safety limits and 48h expiration.</done>
</task>

<task type="auto">
  <name>Task 2: Global Real-time Listeners (Notifications & Credits)</name>
  <files>pickaladder/static/js/notifications.js</files>
  <action>
    Extend `notifications.js` to handle challenges and credits:
    1. Add listener to `db.collection("challenges")` where `participants` contains `currentUserId`. (Note: If `participants` array doesn't exist, we may need to listen to both `challenger_id` and `challenged_id` separately or update 19-01 model).
    2. *Correction*: Use two listeners or update model to have `participant_ids: [uid1, uid2]`. Since 19-01 didn't have it, use:
       - `.where("challenger_id", "==", currentUserId)`
       - `.where("challenged_id", "==", currentUserId)`
    3. Store state in `previousChallengeStatuses = {}`.
    4. On change:
       - If status changed, `showToast` with context (e.g., "Challenge Accepted by [Name]", "New Challenge from [Name]").
       - If `ChallengeUI.refreshHub` is available, call it.
    5. Add listener to `db.collection("users").doc(currentUserId)`:
       - Extract `social_credits`.
       - Update `.credit-balance-nav span` elements globally.
  </action>
  <verify>
    <automated>MISSING — Manual validation in Wave 2. Check logs for listener attachment.</automated>
  </verify>
  <done>Notifications and Navbar Credits stay in sync with Firestore without reloads.</done>
</task>

<task type="auto">
  <name>Task 3: Challenge Hub Reactive Polish</name>
  <files>pickaladder/static/js/challenges.js, pickaladder/templates/match/_issue_challenge_modal.html, pickaladder/templates/match/_challenge_hub.html</files>
  <action>
    1. In `challenges.js`:
       - Remove `location.reload()` from `handleAction`.
       - Ensure `refreshHub()` correctly renders the list from fresh data.
       - Add `ChallengeUI.notifyUpdate()` which can be called by `notifications.js`.
    2. In `_issue_challenge_modal.html`:
       - Add `max="50"` to the wager input.
       - Update help text to reflect 48h expiration.
    3. In `_challenge_hub.html`:
       - Add "Expires in Xh" display to pending items if possible.
  </action>
  <verify>
    <automated>MISSING — Requires visual check.</automated>
  </verify>
  <done>UI is responsive, reactive, and enforces local validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Firestore | Real-time listeners expose challenge metadata. |
| Client -> API | Challenge actions (accept/decline) must be authorized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering | Wager Limit | mitigate | Enforced in `ChallengeService` transaction (server-side). |
| T-19-03-02 | Info Disclosure | Notification Content | mitigate | Only listen to challenges where user is a participant. |
| T-19-03-03 | Denial of Service | Challenge Spamming | mitigate | Enforced max 3 active challenges per user. |
</threat_model>

<verification>
1. Run `pytest tests/test_challenge_safety.py` to ensure limits work.
2. Manually test issuing a challenge: observe Escrow deduction in navbar (real-time).
3. Manually test accepting a challenge from another tab/user: observe toast and Hub refresh (real-time).
4. Verify 48h expiration is stored in Firestore documents.
</verification>

<success_criteria>
- [ ] Users receive toast notifications for all challenge lifecycle events.
- [ ] Navbar credit balance updates immediately when a challenge is issued or resolved.
- [ ] Challenges cannot exceed 50 credits or 3 active slots.
- [ ] Challenge Hub on Dashboard updates without page reloads.
</success_criteria>

<output>
After completion, create `.planning/phases/19-challenges/19-03-SUMMARY.md`
</output>
