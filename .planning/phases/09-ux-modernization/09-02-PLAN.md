---
phase: 09-ux-modernization
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [pickaladder/teams/routes.py, pickaladder/templates/team/wizard.html, pickaladder/teams/services.py]
autonomous: true
requirements: [TEAMUX-02]
must_haves:
  truths:
    - "Users can access /wizard to create a team"
    - "Wizard supports 3 distinct steps with progress feedback"
    - "Roster selection step supports member filtering"
  artifacts:
    - path: "pickaladder/templates/team/wizard.html"
      provides: "Multi-step creation flow"
  key_links:
    - from: "Wizard Form"
      to: "TeamService.create_named_team"
      via: "POST /wizard"
---

<objective>
Implement a simplified 'Team Creation Wizard' (multi-step flow) to reduce cognitive load and friction when creating dynamic named teams.

Purpose: Improve the team formation experience.
Output: A multi-step UI flow for team creation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ux-modernization/RESEARCH.md
@.planning/phases/09-ux-modernization/09-01-SUMMARY.md

<interfaces>
From pickaladder/teams/services.py:
```python
class TeamService:
    @staticmethod
    def create_named_team(db: Client, name: str, creator_id: str, member_ids: list[str]) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard Route & Skeleton Structure</name>
  <files>pickaladder/teams/routes.py, pickaladder/templates/team/wizard.html</files>
  <action>
    - Add `@bp.route("/wizard")` to `pickaladder/teams/routes.py`.
    - Create `wizard.html` with a Bootstrap progress bar and three hidden containers for the steps:
      - Step 1: Identity (Team Name).
      - Step 2: Roster (Member Selection).
      - Step 3: Review & Confirm.
    - Ensure the route passes the current user's groups/members to the template for roster selection.
  </action>
  <verify>
    <automated>python test_syntax.py --path pickaladder/teams/routes.py</automated>
  </verify>
  <done>Wizard route and skeleton UI are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-step Interactive Logic</name>
  <files>pickaladder/templates/team/wizard.html</files>
  <action>
    - Add jQuery logic to handle 'Next' and 'Back' transitions between wizard steps.
    - Update the progress bar width during transitions.
    - Implement a text-based filter in Step 2 to allow searching/filtering the group member list for roster selection.
    - Populate Step 3 with a summary of the team name and selected members before submission.
  </action>
  <verify>
    <automated>python verify_css.py --path templates/team/wizard.html</automated>
  </verify>
  <done>Wizard supports interactive step transitions and member filtering.</done>
</task>

<task type="auto">
  <name>Task 3: Finalization & TeamService Integration</name>
  <files>pickaladder/teams/routes.py</files>
  <action>
    - Implement the POST handler for the Wizard to collect team data.
    - Call `TeamService.create_named_team` with the validated name and member IDs.
    - Redirect to the new team's dashboard upon successful creation.
  </action>
  <verify>
    <automated>pytest tests/test_team_service.py</automated>
  </verify>
  <done>Team creation wizard is fully integrated and functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Team Wizard POST | Data must be validated to ensure all roster members belong to the same group as the creator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | Team Roster IDs | mitigate | Verify that all submitted `member_ids` are valid user IDs in the database. |
| T-09-02-02 | Information Disclosure | Member List | mitigate | Only display members from groups the user already belongs to. |
</threat_model>

<verification>
1. Access `/wizard`, enter a team name, and click 'Next' -> progress bar updates.
2. Search for a member in Step 2 -> list filters correctly.
3. Complete the wizard and verify the new team is created in Firestore and redirects correctly.
</verification>

<success_criteria>
Users can create a named team with a roster through a guided 3-step process with real-time feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ux-modernization/09-02-SUMMARY.md`
</output>
