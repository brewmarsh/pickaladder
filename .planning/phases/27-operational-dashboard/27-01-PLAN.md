---
phase: "27"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pickaladder/services/error_service.py
  - pickaladder/error_handlers.py
  - pickaladder/admin/routes.py
  - pickaladder/admin/services.py
  - pickaladder/templates/admin/layout.html
  - pickaladder/templates/admin/dashboard.html
  - pickaladder/templates/admin/admin.html
autonomous: true
requirements: [OPS-01]

must_haves:
  truths:
    - "Server-side errors (500) are persisted to the system_errors collection"
    - "Admins can access a dedicated Operational Dashboard"
    - "The dashboard displays growth metrics (new users) and recent errors/audit logs"
  artifacts:
    - path: "pickaladder/services/error_service.py"
      provides: "Error persistence logic"
    - path: "pickaladder/templates/admin/dashboard.html"
      provides: "Operational dashboard UI"
  key_links:
    - from: "pickaladder/error_handlers.py"
      to: "pickaladder/services/error_service.py"
      via: "logging call in handle_500"
---

<objective>
Establish the operational monitoring foundation by persisting server-side errors and creating a centralized Admin Dashboard for health and growth visualization.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pickaladder/error_handlers.py
@pickaladder/admin/routes.py
@pickaladder/admin/services.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error Persistence Layer</name>
  <files>pickaladder/services/error_service.py, pickaladder/error_handlers.py</files>
  <action>
    - Create `pickaladder/services/error_service.py` with an `ErrorService` class.
    - Implement `log_error(exception, request_context=None)`:
        - Extract message, stack trace (using `traceback.format_exc()`), URL, user_id, and timestamp.
        - Write to `system_errors` collection in Firestore.
    - Update `pickaladder/error_handlers.py`:
        - Import `ErrorService`.
        - In `handle_500`, call `ErrorService.log_error(e)` before returning the template.
  </action>
  <verify>
    <automated>pytest tests/test_error_persistence.py</automated>
  </verify>
  <done>500 errors are automatically saved to Firestore system_errors collection.</done>
</task>

<task type="auto">
  <name>Task 2: Admin Dashboard Foundation</name>
  <files>pickaladder/templates/admin/layout.html, pickaladder/templates/admin/dashboard.html, pickaladder/admin/routes.py</files>
  <action>
    - Create `pickaladder/templates/admin/layout.html` extending `layout.html` with a sub-navigation bar for Admin (Dashboard, Users, Matches, Style Guide).
    - Create `pickaladder/templates/admin/dashboard.html` extending the new admin layout.
    - Update `pickaladder/admin/routes.py`:
        - Add `@bp.route("/dashboard")` (or repurpose `/` as the dashboard).
        - Fetch growth metrics (count users per day for last 7 days).
        - Fetch latest 5 system errors and latest 5 audit logs.
        - Render `admin/dashboard.html` with this data.
  </action>
  <verify>
    <automated>curl -u admin:password http://localhost:5000/admin/dashboard</automated>
  </verify>
  <done>Admin Dashboard is accessible and displays real data placeholders for logs and errors.</done>
</task>

<task type="auto">
  <name>Task 3: Operational Visualizations</name>
  <files>pickaladder/templates/admin/dashboard.html, pickaladder/admin/services.py</files>
  <action>
    - In `pickaladder/admin/services.py`, implement `get_growth_metrics()` returning daily signup counts.
    - Update `dashboard.html` to use `Chart.js` (already in `layout.html`) to render:
        - A line chart of daily signups.
        - A list/table of recent system errors with "View Details" (modal or link).
        - A list/table of recent audit logs.
  </action>
  <verify>
    <automated>npm run test:e2e -- --spec tests/e2e/admin_dashboard.cy.ts</automated>
  </verify>
  <done>Dashboard renders a growth chart and operational lists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → ErrorService | Stack traces may contain sensitive internal info; ensure system_errors is only readable by admins. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | Error Logs | Mitigate | Enforce strict Firestore rules on `system_errors` to allow only `isAdmin` users to read. |
| T-27-02 | Denial of Service | Error Persistence | Mitigate | Implement basic rate limiting or capping of error logs per minute to prevent DB flooding if a loop occurs. |
</threat_model>

<success_criteria>
- Admins can view a "Dashboard" with at least one Chart.js visualization.
- Triggering a 500 error results in a new document in the `system_errors` collection.
- Audit logs are visible on the main dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/27-operational-dashboard/27-01-SUMMARY.md`
</output>
