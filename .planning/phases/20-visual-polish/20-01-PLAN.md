---
phase: 20-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pickaladder/static/css/variables.css, pickaladder/templates/layout.html, pickaladder/static/style.css, pickaladder/static/css/layout-utils.css]
autonomous: true
requirements: [POLISH-01, POLISH-04]

must_haves:
  truths:
    - "Global CSS variables are clean and used consistently without legacy bridging."
    - "layout.html has zero duplicate Font Awesome imports."
    - "style.css is audited and redundant utility classes are removed."
  artifacts:
    - path: "pickaladder/static/css/variables.css"
      provides: "Unified CSS variable system"
    - path: "pickaladder/templates/layout.html"
      provides: "Cleaned base layout"
  key_links:
    - from: "pickaladder/static/css/variables.css"
      to: "other CSS files"
      via: "var() usage"
---

<objective>
Refine the global style foundation by cleaning up CSS variables, removing redundant code, and ensuring a unified aesthetic baseline.

Purpose: Eliminate technical debt in the styling layer and provide a consistent base for component-level polish.
Output: Refined variables.css, cleaned layout.html, and consolidated utilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pickaladder/static/css/variables.css
@pickaladder/templates/layout.html
@pickaladder/static/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine Global CSS Variables</name>
  <files>pickaladder/static/css/variables.css</files>
  <action>
    - Remove the "BACKWARD COMPATIBILITY BRIDGE" section from variables.css.
    - Standardize naming to use semantic tokens where possible (e.g., --bg-surface instead of --bg-secondary if it makes more sense).
    - Add missing variables for standard hover states and disabled states to prevent hardcoded colors in other files.
    - Ensure all color variables have high-contrast text-on-color counterparts (per D-01: Pro aesthetic).
  </action>
  <verify>
    <automated>grep "BACKWARD COMPATIBILITY BRIDGE" pickaladder/static/css/variables.css || exit 0</automated>
  </verify>
  <done>Variables are refined and legacy bridge is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Layout Cleanup & Redundancy Audit</name>
  <files>pickaladder/templates/layout.html, pickaladder/static/style.css, pickaladder/static/css/layout-utils.css</files>
  <action>
    - Remove duplicate Font Awesome <link> tags in layout.html (lines 23, 38, 42). Keep only the most recent version (6.5.1).
    - Audit style.css for utility classes already provided by Bootstrap 4.5.3 (d-flex, justify-content-between, w-100, mb-0, etc.).
    - Move unique, non-Bootstrap utilities from style.css to layout-utils.css.
    - Ensure all layout-level typography uses --font-family (Inter) for UI and --font-score (Oswald) for scores.
  </action>
  <verify>
    <automated>grep -c "font-awesome" pickaladder/templates/layout.html</automated>
  </verify>
  <done>layout.html is cleaned and style.css redundancies are removed.</done>
</task>

<task type="auto">
  <name>Task 3: Global Spacing & Typography Alignment</name>
  <files>pickaladder/static/css/layout.css, pickaladder/static/css/variables.css</files>
  <action>
    - Review layout.css to ensure it uses the refined variables for margins and paddings.
    - Standardize the container max-width and padding for a consistent "Pro" feel across all views.
    - Apply --font-score to all elements displaying match scores project-wide (grep for score-related classes).
  </action>
  <verify>
    <automated>grep "var(--font-score)" pickaladder/static/css/*.css</automated>
  </verify>
  <done>Global layout spacing and typography are aligned with the design system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CSS | Untrusted client interprets CSS; potential for XSS if CSS is dynamically generated from user input (not applicable here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | CSS Comments | mitigate | Ensure no sensitive comments are left in production CSS files. |
</threat_model>

<verification>
Check visual consistency on Dashboard and Settings pages.
</verification>

<success_criteria>
- No legacy CSS variables in use.
- One single Font Awesome import.
- Consistent typography and spacing project-wide.
</success_criteria>

<output>
After completion, create `.planning/phases/20-visual-polish/20-01-SUMMARY.md`
</output>
