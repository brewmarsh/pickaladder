---
phase: 02-ranking-logic
plan: 01
type: execute
wave: 1
depends_on: [01-02]
files_modified: [
  pickaladder/match/services/record_service.py,
  pickaladder/group/services/leaderboard.py,
  pickaladder/group/services/shootout_service.py,
  tests/test_ranking_integrity.py,
  tests/test_shootout_logic.py
]
autonomous: true
requirements: [RANK-01, RANK-02, QUAL-02]
must_haves:
  truths:
    - "Leaderboards are sorted by ELO/Glicko rating by default"
    - "Leaderboard calculations use denormalized user stats (performance fix)"
    - "Shootout logic correctly pairs players and assigns courts based on performance"
    - "Ranking updates are verified by integration tests"
  artifacts:
    - path: "pickaladder/group/services/shootout_service.py"
      provides: "Automated court movement algorithm"
    - path: "tests/test_ranking_integrity.py"
      provides: "Verification of ELO/Glicko correctness"
---

<objective>
Professionalize the ranking system by implementing ELO-first leaderboards and a "Shootout" service for automated court movement. Resolve the O(U*M) performance bottleneck in leaderboard calculation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/audit/AUDIT_REPORT.md
@pickaladder/match/services/calculator.py
@pickaladder/match/services/record_service.py
@pickaladder/group/services/leaderboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ELO-First Global Leaderboard (RANK-01)</name>
  <files>pickaladder/match/services/record_service.py</files>
  <action>
    Refactor `MatchRecordService.get_leaderboard_data` to use denormalized stats.
    - Instead of streaming all matches, sort users by `stats.elo` (or `duprRating` as fallback).
    - Ensure it honors the `min_games` requirement using the `stats.wins + stats.losses` from user documents.
    - This fixes the O(U*M) performance issue.
  </action>
  <verify>
    <automated>python -c "from pickaladder.match.services.record_service import MatchRecordService; print('Success')"</automated>
  </verify>
  <done>Global leaderboard now uses denormalized ELO stats for O(1) per-user ranking access.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ELO-First Group Leaderboard (RANK-01)</name>
  <files>pickaladder/group/services/leaderboard.py</files>
  <action>
    Refactor `get_group_leaderboard` to prioritize ELO.
    - Fetch group members and their denormalized stats.
    - Change default sort from `avg_score` to ELO rating.
    - Retain historical rank change logic if possible, or adapt it to ELO changes.
  </action>
  <verify>
    <automated>grep -q "elo" pickaladder/group/services/leaderboard.py</automated>
  </verify>
  <done>Group leaderboards now reflect player skill via ELO ratings.</done>
</task>

<task type="auto">
  <name>Task 3: Create Shootout Service (RANK-02)</name>
  <files>pickaladder/group/services/shootout_service.py</files>
  <action>
    Implement the "Shootout" court movement algorithm.
    - Create `ShootoutService` with a method to calculate "next court assignments".
    - Logic: Group players into courts of 4. Winners move up a court, losers move down.
    - Handle edge cases (top court winners stay, bottom court losers stay).
    - Use match results from the current session/group to determine movement.
  </action>
  <verify>
    <automated>ls pickaladder/group/services/shootout_service.py</automated>
  </verify>
  <done>Automated court movement service is implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Ranking & Shootout Integration Tests (QUAL-02)</name>
  <files>tests/test_ranking_integrity.py, tests/test_shootout_logic.py</files>
  <action>
    Add comprehensive tests for the new ranking and movement logic.
    - `test_ranking_integrity.py`: Verify ELO updates correctly after matches and leaderboards sort properly.
    - `test_shootout_logic.py`: Verify players move between courts correctly based on simulated win/loss patterns.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ranking_integrity.py tests/test_shootout_logic.py</automated>
  </verify>
  <done>Ranking and movement logic is verified with automated integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Match Record -> User Stats | Batch update integrity |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | ELO Ratings | mitigate | Ensure ranking updates only happen via MatchCommandService batch writes |
| T-02-01-02 | Denial of Service | Leaderboard Calculation | fixed | Replaced O(U*M) calculation with O(U log U) sort on indexed fields |
</threat_model>

<success_criteria>
1. Global and Group leaderboards sort by ELO by default.
2. Leaderboard performance is significantly improved (no match-streaming per user).
3. Shootout service correctly calculates court movement for at least 8 players (2 courts).
4. Integration tests pass for ranking and shootout logic.
</success_criteria>

<output>
After completion, create .planning/phases/02-ranking-logic/02-01-SUMMARY.md
</output>
