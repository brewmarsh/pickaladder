---
phase: 24-async-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pickaladder/core/tasks.py, pickaladder/main/routes.py, pickaladder/__init__.py]
autonomous: true
requirements: [QUAL-04, QUAL-05]

must_haves:
  truths:
    - "Application has a functional ThreadPoolExecutor for background tasks"
    - "Health endpoint returns 200 OK and status JSON"
    - "Background tasks have access to the Flask application context"
  artifacts:
    - path: "pickaladder/core/tasks.py"
      provides: "Async task execution infrastructure"
    - path: "pickaladder/main/routes.py"
      provides: "/health endpoint"
  key_links:
    - from: "pickaladder/__init__.py"
      to: "pickaladder/core/tasks.py"
      via: "Initialization in create_app"
---

<objective>
Establish the core infrastructure for asynchronous background processing and system health monitoring. This provides a lightweight way to run long-running tasks without blocking the main request-response cycle.

Purpose: Improve application responsiveness and observability.
Output: Task execution utility and a standard health check endpoint.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pickaladder/__init__.py
@pickaladder/main/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TaskExecutor in pickaladder/core/tasks.py</name>
  <files>pickaladder/core/tasks.py</files>
  <action>
    Create a `TaskExecutor` class that wraps `concurrent.futures.ThreadPoolExecutor`.
    - Use `max_workers` from config (default to 4).
    - Implement an `init_app(app)` method to store the app instance.
    - Implement `run_async(func, *args, **kwargs)` that submits a wrapper to the executor.
    - The wrapper MUST use `with self.app.app_context():` to ensure the task has access to Flask extensions and config.
    - Add logging for task submission and completion (or errors).
  </action>
  <verify>
    <automated>python -c "from pickaladder.core.tasks import TaskExecutor; from flask import Flask; app = Flask(__name__); executor = TaskExecutor(app); fut = executor.run_async(lambda: 'success'); print(fut.result())" | grep "success"</automated>
  </verify>
  <done>TaskExecutor class implemented and verified with a standalone test.</done>
</task>

<task type="auto">
  <name>Task 2: Add /health endpoint to pickaladder/main/routes.py</name>
  <files>pickaladder/main/routes.py</files>
  <action>
    Add a new route `@bp.route("/health")` that returns a JSON response: `{"status": "healthy"}`.
    Ensure it returns a 200 OK status code.
    This endpoint will be used by monitoring tools to verify the application is up and running.
  </action>
  <verify>
    <automated>pytest tests/test_app.py -k "health" || curl -I http://localhost:5000/health</automated>
  </verify>
  <done>/health endpoint returns 200 OK and "healthy" status.</done>
</task>

<task type="auto">
  <name>Task 3: Register TaskExecutor in create_app</name>
  <files>pickaladder/__init__.py, pickaladder/extensions.py</files>
  <action>
    - Add `executor = TaskExecutor()` to `pickaladder/extensions.py`.
    - In `pickaladder/__init__.py`, import `executor` from `.extensions`.
    - In `_register_extensions(app)`, call `executor.init_app(app)`.
    - Ensure `executor` is accessible via `current_app.extensions['task_executor']` or just by importing from `extensions`.
  </action>
  <verify>
    <automated>python -c "from pickaladder import create_app; app = create_app(); from pickaladder.extensions import executor; print(executor.app == app)" | grep "True"</automated>
  </verify>
  <done>TaskExecutor is initialized with the Flask app during startup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal -> Threads | Data passed to background tasks must be thread-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Information Disclosure | /health | accept | Endpoint returns no PII or sensitive system info, only status. |
| T-24-01-02 | Denial of Service | TaskExecutor | mitigate | Limit max_workers to prevent resource exhaustion. |
</threat_model>

<verification>
Check that /health returns 200.
Verify that a background task can be submitted and finishes successfully.
</verification>

<success_criteria>
- TaskExecutor is functional and integrated.
- /health endpoint is live.
- Background tasks have application context.
</success_criteria>

<output>
After completion, create `.planning/phases/24-async-processing/24-01-SUMMARY.md`
</output>
