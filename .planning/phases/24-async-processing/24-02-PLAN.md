---
phase: 24-async-processing
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified: [pickaladder/services/mail_service.py, pickaladder/services/notification_service.py, pickaladder/utils.py, pickaladder/auth/routes.py, pickaladder/group/utils.py, pickaladder/user/services/profile.py, pickaladder/tournament/services/tournament_service.py]
autonomous: true
requirements: [QUAL-04]

must_haves:
  truths:
    - "Emails are sent asynchronously without blocking the web request"
    - "Push notifications can be dispatched to FCM tokens in the background"
    - "Email logic is centralized in a dedicated service"
  artifacts:
    - path: "pickaladder/services/mail_service.py"
      provides: "Async email dispatch service"
    - path: "pickaladder/services/notification_service.py"
      provides: "Async FCM notification dispatch service"
  key_links:
    - from: "pickaladder/services/mail_service.py"
      to: "pickaladder.extensions.executor"
      via: "Background task submission"
---

<objective>
Refactor critical communication services to utilize background processing, significantly improving request response times for operations like user registration, group invites, and tournament results.

Purpose: Offload high-latency I/O (SMTP and FCM) to background threads.
Output: Async-first Email and Notification services.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-async-processing/24-01-SUMMARY.md
@pickaladder/utils.py
@pickaladder/auth/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Async MailService</name>
  <files>pickaladder/services/mail_service.py, pickaladder/utils.py</files>
  <action>
    - Create `pickaladder/services/mail_service.py`.
    - Move `send_email` logic from `pickaladder/utils.py` to `MailService.send_email_now`.
    - Implement `MailService.send_email` (public method) which uses `executor.run_async` to call `send_email_now`.
    - Ensure `MailService` handles logging of background task success/failure.
    - Update `pickaladder/utils.py` to import `send_email` from the new service or maintain it as a wrapper for backward compatibility.
  </action>
  <verify>
    <automated>pytest tests/test_auth.py -k "registration"  # Verify registration still "works" (even if async)</automated>
  </verify>
  <done>Email sending is refactored into an async service.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Async NotificationService (FCM)</name>
  <files>pickaladder/services/notification_service.py</files>
  <action>
    - Create `pickaladder/services/notification_service.py`.
    - Implement `NotificationService.send_push_notification` using `firebase_admin.messaging`.
    - Wrap the actual sending in `executor.run_async`.
    - Implement `send_to_user(user_id, title, body, data)` which fetches the user's `fcmToken` from Firestore and dispatches the notification.
    - Handle cases where the user has no token gracefully (just log and skip).
  </action>
  <verify>
    <automated>python -c "from pickaladder.services.notification_service import NotificationService; print(hasattr(NotificationService, 'send_to_user'))" | grep "True"</automated>
  </verify>
  <done>FCM notification service is implemented and background-ready.</done>
</task>

<task type="auto">
  <name>Task 3: Update callers and verify async behavior</name>
  <files>pickaladder/auth/routes.py, pickaladder/group/utils.py, pickaladder/user/services/profile.py, pickaladder/tournament/services/tournament_service.py</files>
  <action>
    - Update all imports of `send_email` to point to the new service if necessary.
    - Add logging to the `TaskExecutor` or services to confirm that tasks are indeed running in separate threads.
    - Verify that a slow SMTP server would no longer block the HTTP response (can be mocked in tests).
  </action>
  <verify>
    <automated>grep -r "send_email" pickaladder/ | grep -v "mail_service.py"</automated>
  </verify>
  <done>All system email calls are now non-blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> SMTP/FCM | Outgoing sensitive info must be encrypted (TLS/HTTPS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | Email Content | mitigate | Use server-side templates (Jinja2) for email generation. |
| T-24-02-02 | Information Disclosure | FCM Tokens | mitigate | Ensure tokens are only accessible by the owner and admins. |
</threat_model>

<verification>
Perform a user registration and verify that the "Welcome" email is triggered (check logs for background task execution).
Verify that no synchronous errors are raised if SMTP fails (should be logged in the background).
</verification>

<success_criteria>
- User registration completes instantly regardless of SMTP latency.
- FCM notifications can be sent via a simple service call.
- Background task failures are visible in the application logs.
</success_criteria>

<output>
After completion, create `.planning/phases/24-async-processing/24-02-SUMMARY.md`
</output>
