---
phase: 03-external-integration
plan: 01
type: execute
wave: 1
depends_on: [02-01]
files_modified: [
  pickaladder/user/services/core.py,
  pickaladder/match/services/command.py,
  pickaladder/match/services/query.py,
  pickaladder/user/models.py,
  pickaladder/match/models.py,
  pickaladder/config.py
]
autonomous: true
requirements: [DUPR-01, DUPR-02, RANK-03]
must_haves:
  truths:
    - "Users can link and sync their DUPR ID/Rating"
    - "Matches involving DUPR-linked players are marked as 'Verified'"
    - "Inactive players' rankings are handled via decay or activity checks"
  artifacts:
    - path: "pickaladder/user/services/dupr_service.py"
      provides: "DUPR API integration and sync logic"
---

<objective>
Connect pickaladder to the broader pickleball ecosystem via DUPR integration and ensure long-term leaderboard health with rank decay logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@pickaladder/user/models.py
@pickaladder/match/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DUPR Integration Service (DUPR-01)</name>
  <files>pickaladder/user/services/dupr_service.py, pickaladder/config.py</files>
  <action>
    Create a service for DUPR API interaction.
    - Add `DUPR_API_KEY` and `DUPR_BASE_URL` to `pickaladder/config.py`.
    - Implement `DUPRService` to fetch ratings for a given `dupr_id`.
    - Add a "Sync DUPR" method to `UserService` that updates a user's `dupr_rating` in Firestore.
  </action>
  <verify>
    <automated>ls pickaladder/user/services/dupr_service.py</automated>
  </verify>
  <done>DUPR integration service is ready for rating synchronization.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Verified Match Status (DUPR-02)</name>
  <files>pickaladder/match/services/command.py, pickaladder/match/models.py</files>
  <action>
    Automatically mark matches as 'verified' if participants have DUPR IDs.
    - Update `MatchCommandService.record_match` to check if all participants have a `dupr_id`.
    - Set `is_verified: true` in the match document if the condition is met.
    - Update `Match` and `MatchResult` models to include `is_verified`.
  </action>
  <verify>
    <automated>grep -q "is_verified" pickaladder/match/services/command.py</automated>
  </verify>
  <done>Matches are now automatically verified based on participant DUPR status.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Rank Decay Logic (RANK-03)</name>
  <files>pickaladder/match/services/record_service.py</files>
  <action>
    Implement activity-based ranking penalties.
    - Create a helper in `MatchRecordService` that checks the date of a user's last match.
    - If a user hasn't played in X days (e.g., 30 days), apply a penalty to their ELO in the leaderboard view (or flag them as inactive).
    - This ensures top players must remain active to maintain their rank.
  </action>
  <verify>
    <automated>grep -q "decay" pickaladder/match/services/record_service.py</automated>
  </verify>
  <done>Rank decay logic is implemented to ensure leaderboard activity.</done>
</task>

<task type="auto">
  <name>Task 4: Final Validation & Cleanup</name>
  <files>tests/test_dupr_sync.py, tests/test_rank_decay.py</files>
  <action>
    Add final integration tests for Phase 3.
    - `test_dupr_sync.py`: Verify DUPR rating sync updates user documents.
    - `test_rank_decay.py`: Verify inactive users are penalized or flagged correctly.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dupr_sync.py tests/test_rank_decay.py</automated>
  </verify>
  <done>Phase 3 features are verified with automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DUPR API -> Flask | External data source integrity |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | DUPR ID | mitigate | Optional: Implement DUPR account verification if API allows |
| T-03-01-02 | Information Disclosure | DUPR API Key | fixed | Moved to Config class / Environment variables |
</threat_model>

<success_criteria>
1. Users can manually trigger a DUPR rating sync.
2. Matches with DUPR-linked players are visually/data-flagged as 'Verified'.
3. Leaderboard accounts for player inactivity via ranking penalties.
4. Integration tests pass for all Phase 3 requirements.
</success_criteria>

<output>
After completion, create .planning/phases/03-external-integration/03-01-SUMMARY.md
</output>
