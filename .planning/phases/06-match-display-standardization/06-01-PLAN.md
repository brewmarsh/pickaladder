---
phase: 06-match-display-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pickaladder/static/css/data-displays.css
  - pickaladder/templates/components/match_list_item.html
  - pickaladder/templates/match/summary.html
  - pickaladder/templates/components/_recent_matches.html
autonomous: false
requirements:
  - DISPLAY-01
  - DISPLAY-02
  - DISPLAY-03
  - DISPLAY-04
  - DISPLAY-05

must_haves:
  truths:
    - "Match wins use Volt background with dark text"
    - "Match losses use dark background"
    - "Match scores across the app use the Oswald font family"
    - "All primary match display components use standardized CSS classes"
  artifacts:
    - path: "pickaladder/static/css/data-displays.css"
      provides: "Standardized status and typography classes"
      contains: [".status-win", ".status-loss", ".font-score"]
    - path: "pickaladder/templates/components/match_list_item.html"
      provides: "Consistent match item display"
      contains: ["status-win", "status-loss"]
    - path: "pickaladder/templates/match/summary.html"
      provides: "Standardized match summary"
      contains: ["status-win", "status-loss", "font-score"]
  key_links:
    - from: "pickaladder/templates/components/match_list_item.html"
      to: "pickaladder/static/css/data-displays.css"
      via: "CSS classes"
      pattern: "status-(win|loss)"
---

<objective>
Implement a consistent High Contrast (Volt/Black) match display across the application. This involves defining standardized CSS classes for match outcomes and enforcing specialized typography for scores to align with the "Court & Volt" design system.

Purpose: Professionalize the match history and leaderboard UI with a high-visibility, branded theme.
Output: Standardized CSS and refactored match display templates.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-match-display-standardization/RESEARCH.md

<interfaces>
From pickaladder/static/css/variables.css (Existing):
```css
:root {
    --color-volt: #ccff00;
    --text-primary: #111827;
    --text-on-accent: #000000;
    --bg-secondary: #f3f4f6;
    --font-score: 'Oswald', sans-serif;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define CSS & Typography Classes (Wave 1)</name>
  <files>pickaladder/static/css/data-displays.css</files>
  <action>
    Define standardized match status and typography classes in `data-displays.css` per RESEARCH.md:
    1. `.status-win`: Use `var(--color-volt)` for background and `var(--text-on-accent)` for text. Set `font-weight: bold` and `text-transform: uppercase`.
    2. `.status-loss`: Use `var(--text-primary)` for background and `var(--bg-secondary)` (or light text) for text. Set `font-weight: bold`, `text-transform: uppercase`, and a subtle border.
    3. `.font-score`: Apply `font-family: var(--font-score)`.
    Ensure `!important` is used where necessary to override default Bootstrap badge styles.
  </action>
  <verify>
    <automated>grep -E ".status-win|.status-loss|.font-score" pickaladder/static/css/data-displays.css</automated>
  </verify>
  <done>Standardized CSS classes are available for use in templates.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Match Templates (Wave 2)</name>
  <files>
    pickaladder/templates/components/match_list_item.html,
    pickaladder/templates/match/summary.html,
    pickaladder/templates/components/_recent_matches.html
  </files>
  <action>
    Refactor match display components to use the new standardized classes:
    1. In `match_list_item.html`: Replace `badge-success`/`badge-danger` (or similar) with `status-win`/`status-loss`. Apply `font-score` to the score display.
    2. In `summary.html`: Update the result header and score cards to use `status-win`/`status-loss` and `font-score`.
    3. In `_recent_matches.html`: Update the recent match badges to use the new standardized classes.
    Ensure that logic correctly assigns `status-win` if the user won and `status-loss` if they lost.
  </action>
  <verify>
    <automated>grep -E "status-win|status-loss|font-score" pickaladder/templates/components/match_list_item.html pickaladder/templates/match/summary.html pickaladder/templates/components/_recent_matches.html</automated>
  </verify>
  <done>All primary match display components use the standardized High Contrast theme.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual Verification of Theme</name>
  <action>Perform visual inspection of the match displays across the dashboard and summary pages.</action>
  <verify>Manual confirmation of visual fidelity.</verify>
  <done>User approves the new High Contrast theme.</done>
  <what-built>High Contrast (Volt/Black) match displays across the app.</what-built>
  <how-to-verify>
    1. Start the application.
    2. Navigate to the Dashboard or User Profile.
    3. Verify that the "Recent Matches" list shows Volt "W" badges for wins and Black badges for losses.
    4. Verify that scores are rendered in the Oswald font.
    5. Click on a match to view the Summary and verify consistency there.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-side Template → Browser | Match data (scores, results) is rendered in HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Templates | mitigate | Jinja2 auto-escaping is active, preventing XSS from match comments or player names. |
| T-06-02 | Information Disclosure | CSS | accept | CSS classes are public and contain no sensitive information. |
| T-06-03 | Spoofing | Match Data | mitigate | Ensure user session is verified before displaying personalized match results (existing logic). |
</threat_model>

<verification>
- Standardized classes `.status-win`, `.status-loss`, and `.font-score` exist in `data-displays.css`.
- `match_list_item.html` uses the new classes for win/loss indicators.
- `summary.html` uses the new classes and score typography.
- `_recent_matches.html` uses the new classes.
- Visual inspection confirms High Contrast Volt/Black theme.
</verification>

<success_criteria>
- [ ] `data-displays.css` updated with new classes.
- [ ] `match_list_item.html` refactored.
- [ ] `summary.html` refactored.
- [ ] `_recent_matches.html` refactored.
- [ ] All score displays use Oswald font via `font-score`.
- [ ] Visual verification approved by user.
</success_criteria>

<output>
After completion, create `.planning/phases/06-match-display-standardization/06-01-SUMMARY.md`
</output>
