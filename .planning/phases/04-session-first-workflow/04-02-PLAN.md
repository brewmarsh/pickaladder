---
phase: 04-session-first-workflow
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [pickaladder/group/routes.py, pickaladder/templates/group/quick_log.html]
autonomous: false
requirements: [BATCH-02, BATCH-03, BATCH-04]
user_setup: []

must_haves:
  truths:
    - "User can access a high-contrast 'Quick Log' view for an active session"
    - "Player selection for matches is limited to the session pool by default"
    - "User can record a match using a 'Winner-First' 2-tap scoring flow"
  artifacts:
    - path: "pickaladder/templates/group/quick_log.html"
      provides: "Mobile-optimized session match entry UI"
    - path: "pickaladder/group/routes.py"
      provides: "Session-specific UI endpoints"
  key_links:
    - from: "pickaladder/templates/group/quick_log.html"
      to: "MatchCommandService"
      via: "form submission with sessionId"
---

<objective>
Implement the "Quick Log" mobile-optimized UI for session-based match recording, focusing on high-contrast design and a streamlined scoring workflow.

Purpose: Reduce courtside data entry friction.
Output: Quick Log UI and associated routes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-session-first-workflow/RESEARCH.md
@pickaladder/group/routes.py
@pickaladder/templates/record_match.html

<interfaces>
From RESEARCH.md:
- BATCH-03: Winner-First 2-tap scoring shortcuts.
- BATCH-04: Mobile-optimized "Quick Log" view (high contrast/large targets).

From Plan 04-01:
- SessionService.get_session(db, session_id) returns {playerIds: [...], ...}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Quick Log routes and initial template</name>
  <files>pickaladder/group/routes.py, pickaladder/templates/group/quick_log.html</files>
  <action>
    - In pickaladder/group/routes.py: Add a route `/session/<session_id>/quick-log` that fetches session data and renders `group/quick_log.html`.
    - Create `pickaladder/templates/group/quick_log.html`:
      - Use high-contrast colors (e.g., black/white or high-contrast theme).
      - Display player pool as large, clickable buttons/cards.
      - Implement basic layout for doubles (4 players) and singles (2 players).
  </action>
  <verify>
    <automated>curl -I http://localhost:5000/session/TEST_SESSION_ID/quick-log</automated>
  </verify>
  <done>Quick Log route exists and renders the template.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Winner-First scoring and pool selection logic</name>
  <files>pickaladder/templates/group/quick_log.html</files>
  <action>
    Implement JavaScript/Frontend logic in `quick_log.html`:
    - Selection Flow: User taps 2 players (winners) -> User enters loser score -> Confirm.
    - Match Type: Default to session's `matchTypeDefault`.
    - Player Pool: Only players from the session's `playerIds` are selectable by default.
    - Layout: Optimize for outdoor use (large tap targets, minimal text entry).
  </action>
  <verify>
    <automated>npm run test:e2e -- --filter=quick_log # Assuming E2E exists, or manual check</automated>
  </verify>
  <done>Frontend logic supports the 2-tap scoring workflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Quick Log UI with high-contrast design and 2-tap scoring logic.</what-built>
  <how-to-verify>
    1. Navigate to a session's Quick Log view on a mobile device or simulator.
    2. Verify the design is high-contrast and easy to read.
    3. Test the selection flow: tap two players, enter a score, and verify the UI updates correctly for submission.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Wire Quick Log to MatchCommandService</name>
  <files>pickaladder/group/routes.py, pickaladder/templates/group/quick_log.html</files>
  <action>
    - Implement the form submission in `quick_log.html` to POST to the match recording endpoint.
    - Ensure `session_id` is passed in the payload.
    - In `routes.py` (or match routes), ensure the submission correctly utilizes the updated `MatchCommandService` from Plan 01.
    - After successful recording, redirect back to the Quick Log view for the next match (BATCH-02 sequential entry).
  </action>
  <verify>
    <automated>pytest tests/test_match_recording.py</automated>
  </verify>
  <done>Recorded matches are correctly linked to the active session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Match API | Submission of session-linked match data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | Match Creator | mitigate | Verify current_user is a participant or owner of the session before allowing recording. |
</threat_model>

<verification>
Manual verification of the mobile workflow and automated tests for match recording with session IDs.
</verification>

<success_criteria>
- High-contrast Quick Log view is accessible.
- Matches can be recorded with 2-tap scoring from the session pool.
- Sequential match entry allows rapid recording of multiple games.
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-first-workflow/04-02-SUMMARY.md`
</output>
