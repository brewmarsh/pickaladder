---
phase: 04-session-first-workflow
plan: 01
type: execute
wave: 1
depends_on: [03-01]
files_modified: [
  pickaladder/match/models.py,
  pickaladder/match/services/command.py,
  pickaladder/group/services/session_service.py,
  tests/test_session_service.py
]
autonomous: true
requirements: [BATCH-01, BATCH-02]
must_haves:
  truths:
    - "Sessions collection schema supports player pools and match linkage"
    - "MatchCommandService supports optional session_id"
    - "SessionService can create sessions and add matches to them"
  artifacts:
    - path: "pickaladder/group/services/session_service.py"
      provides: "Core logic for session management"
    - path: "tests/test_session_service.py"
      provides: "Unit tests for session logic"
---

<objective>
Implement the core Session-First backend services and extend match models to support session linkage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-session-first-workflow/RESEARCH.md
@pickaladder/match/models.py
@pickaladder/match/services/command.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Match Models & Command Service</name>
  <files>pickaladder/match/models.py, pickaladder/match/services/command.py</files>
  <action>
    Add `session_id` to Match-related models and update recording logic.
    - Update `Match`, `MatchSubmission`, and `MatchResult` in `pickaladder/match/models.py` to include optional `sessionId` / `session_id`.
    - Update `MatchCommandService.record_match` in `pickaladder/match/services/command.py` to accept and store `sessionId`.
  </action>
  <verify>
    <automated>grep -q "session_id" pickaladder/match/models.py</automated>
  </verify>
  <done>Match models and recording logic are session-aware.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SessionService Core</name>
  <files>pickaladder/group/services/session_service.py</files>
  <action>
    Create the SessionService to manage player pools and session lifecycle.
    - Implement `create_session(db, group_id, creator_id, player_ids)`.
    - Implement `get_session(db, session_id)`.
    - Implement `add_match_to_session(db, session_id, match_id)`.
  </action>
  <verify>
    <automated>ls pickaladder/group/services/session_service.py</automated>
  </verify>
  <done>SessionService core logic is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Unit Tests for Session Logic</name>
  <files>tests/test_session_service.py</files>
  <action>
    Create and run unit tests for the new SessionService.
    - Test session creation and retrieval.
    - Test adding matches to a session and verifying linkage.
  </action>
  <verify>
    <automated>uv run pytest tests/test_session_service.py</automated>
  </verify>
  <done>Session logic is verified with unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Session -> Match | Integrity of match linkage |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Session Pool | mitigate | Ensure only group members can be added to a session |
| T-04-01-02 | Information Disclosure | Session Data | fixed | Enforce group-based access control in SessionService |
</threat_model>

<success_criteria>
1. Match models support `session_id`.
2. `SessionService` can create and manage sessions in Firestore.
3. Unit tests pass for all core session operations.
</success_criteria>

<output>
After completion, create .planning/phases/04-session-first-workflow/04-01-SUMMARY.md
</output>
