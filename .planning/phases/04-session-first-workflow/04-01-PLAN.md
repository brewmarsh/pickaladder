---
phase: 04-session-first-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pickaladder/group/services/session_service.py, pickaladder/match/models.py, pickaladder/match/services/command.py, tests/test_session_service.py]
autonomous: true
requirements: [BATCH-01, BATCH-02]
user_setup: []

must_haves:
  truths:
    - "Sessions can be created for a group with a designated player pool"
    - "Players can be added to or removed from an active session's pool"
    - "Matches can be associated with a session ID at the database level"
  artifacts:
    - path: "pickaladder/group/services/session_service.py"
      provides: "Session lifecycle management"
    - path: "pickaladder/match/models.py"
      provides: "Session-aware MatchSubmission model"
    - path: "tests/test_session_service.py"
      provides: "Automated verification of session logic"
  key_links:
    - from: "pickaladder/match/services/command.py"
      to: "sessions collection"
      via: "sessionId field in match document"
---

<objective>
Implement the core backend services for Session-First workflows, including session lifecycle management and extending the match recording service to support session linkage.

Purpose: Provides the foundational data layer for batch match recording.
Output: SessionService, updated MatchCommandService, and unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-session-first-workflow/RESEARCH.md
@pickaladder/match/services/command.py
@pickaladder/match/models.py

<interfaces>
From RESEARCH.md:
Session Document Schema:
- groupId: string
- ownerId: string
- playerIds: array<string>
- matchIds: array<string>
- status: string ('active', 'completed')
- matchTypeDefault: string ('singles', 'doubles')
- createdAt: timestamp
- updatedAt: timestamp
- verifiedBy: array<string>

From pickaladder/match/models.py:
```python
@dataclass
class MatchSubmission:
    match_type: str
    player_1_id: str
    player_2_id: str
    score_p1: int
    score_p2: int
    match_date: Any
    partner_id: str | None = None
    opponent_2_id: str | None = None
    group_id: str | None = None
    tournament_id: str | None = None
    created_by: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionService for lifecycle management</name>
  <files>pickaladder/group/services/session_service.py</files>
  <action>
    Create SessionService class with the following methods:
    - create_session(db, group_id, owner_id, player_ids, match_type='doubles'): Initialize a session document in 'sessions' collection.
    - get_session(db, session_id): Retrieve session data.
    - add_player_to_pool(db, session_id, player_id): Add a player to playerIds array using ArrayUnion.
    - remove_player_from_pool(db, session_id, player_id): Remove a player from playerIds array using ArrayRemove.
    - list_group_sessions(db, group_id, status='active'): List sessions for a group.
    - add_match_to_session(db, session_id, match_id): Internal helper to link match_id.
    
    Ensure it uses firebase_admin.firestore for server timestamps and array operations.
  </action>
  <verify>
    <automated>pytest tests/test_session_service.py</automated>
  </verify>
  <done>SessionService implements CRUD and pool management with Firestore.</done>
</task>

<task type="auto">
  <name>Task 2: Update Match models and CommandService for session support</name>
  <files>pickaladder/match/models.py, pickaladder/match/services/command.py</files>
  <action>
    - In pickaladder/match/models.py: Add `session_id: str | None = None` to `MatchSubmission` dataclass. Add `sessionId: str | None = None` to `MatchResult`.
    - In pickaladder/match/services/command.py:
      - Update `_prepare_match_doc_base` to include `sessionId` in the document data if `sub.session_id` is present.
      - Update `_record_match_batch` to also update the session document's `matchIds` list using `ArrayUnion([match_ref.id])` and update session's `updatedAt`.
      - Update `_build_match_result` to populate `sessionId`.
  </action>
  <verify>
    <automated>pytest tests/test_match_command.py</automated>
  </verify>
  <done>Match recording service supports linking matches to sessions.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Unit Tests for SessionService</name>
  <files>tests/test_session_service.py</files>
  <action>
    Create unit tests using mock-firestore (or standard test db setup) to verify:
    - Session creation with correct initial state.
    - Adding/removing players from the pool.
    - Linking a match ID to a session.
    - Listing active sessions for a group.
  </action>
  <verify>
    <automated>pytest tests/test_session_service.py</automated>
  </verify>
  <done>Unit tests pass for SessionService logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Session creation and pool updates from untrusted user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Session pool | mitigate | Validate that players being added to a session pool are members of the associated group. |
| T-04-01-02 | Information Disclosure | Session data | mitigate | Ensure get_session and list_group_sessions check that the requester is a member of the group. |
</threat_model>

<verification>
Run `pytest tests/test_session_service.py` and `pytest tests/test_match_command.py` to ensure core logic and integration work as expected.
</verification>

<success_criteria>
- SessionService can create and manage session pools in Firestore.
- MatchCommandService correctly links matches to sessions and updates the session document.
- All unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-first-workflow/04-01-SUMMARY.md`
</output>
