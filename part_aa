{% extends "layout.html" %}
{% block title %}Admin Panel{% endblock %}
{% block content %}
    <div class="page-header">
        <h1>Admin Panel</h1>
    </div>
    <div class="card-grid">
        <div class="card">
            <h3>Account Management</h3>
            <p>Advanced tools for managing user accounts and merging data.</p>
            <div style="display: flex; flex-direction: column; gap: 10px;">
                <a href="{{ url_for('admin.merge_players') }}" class="btn btn-warning">Merge Players (Deep Merge)</a>

                <hr>

                <h4>Merge Ghost by Email</h4>
                <form action="{{ url_for('admin.merge_ghost') }}" method="post">
                    <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
                    <div class="form-group">
                        <label for="target_user_id">Real User ID</label>
                        <input type="text" id="target_user_id" name="target_user_id" class="form-control" placeholder="e.g. user_123" required>
                    </div>
                    <div class="form-group">
                        <label for="ghost_email">Ghost Account Email</label>
                        <input type="email" id="ghost_email" name="ghost_email" class="form-control" placeholder="march@gmail.com" required>
                    </div>
                    <button type="submit" class="btn btn-secondary">Merge Ghost Account</button>
                </form>
            </div>
        </div>
        <div class="card">
            <h3>Friend Graph</h3>
            <p>View a visual graph of friend connections.</p>
            <button type="button" class="btn" data-toggle="modal" data-target="#friendGraphModal">
                View Graph
            </button>
        </div>
        <div class="card">
            <h3>Enforce Email Verification</h3>
            <p>
                Current status:
                {% if email_verification_setting and email_verification_setting.value == 'true' %}
                    <strong style="color: green;">Enabled</strong>
                {% else %}
                    <strong style="color: red;">Disabled</strong>
                {% endif %}
            </p>
            <form action="{{ url_for('admin.toggle_email_verification') }}" method="post">
                <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">
                {% if email_verification_setting and email_verification_setting.value == 'true' %}
